#!/bin/bash
# shell/bash/bash_profile - bash ログインシェル用
#
# 配置先: ~/.bash_profile (シンボリックリンク)
# 読み込み: ログインシェル起動時のみ
#
# 役割:
#   - ログイン時のみ必要な処理
#   - .bashrc の読み込み

# SC1091: リンク先/環境依存の外部ファイルを source するため追跡不可を許容
# shellcheck disable=SC1091

# ============================================================================
# .bashrc を読み込む
# ============================================================================

if [[ -f "$HOME/.bashrc" ]]; then
    source "$HOME/.bashrc"
fi

# ============================================================================
# ログイン時のみの処理
# ============================================================================

# 最終ログイン情報などを表示したい場合はここに追加

# ============================================================================
# ローカル設定 (Git管理外)
# ============================================================================

if [[ -f "$HOME/.bash_profile.local" ]]; then
    source "$HOME/.bash_profile.local"
fi
