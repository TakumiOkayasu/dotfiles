# ブランチ後片付け

マージ後の後片付けを実行する。

## トリガー

- `/branch-cleanup`
- 「後片付けして」
- 「ブランチ整理」

## 実行内容

```bash
git checkout main && git pull
```

**注意**: `git pull`後、hookがマージ済みローカルブランチを自動削除する。

## 出力

```
✅ 後片付け完了

| 項目 | 結果 |
|------|------|
| ブランチ | main |
| 削除 | [削除されたブランチ名] |
```
