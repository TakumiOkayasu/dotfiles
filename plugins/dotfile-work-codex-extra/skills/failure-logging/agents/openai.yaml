interface:
  display_name: "Failure Logging"
  short_description: "Optional workflow skill. Explicit invocation recommended."
  default_prompt: "Use $failure-logging for this task."
policy:
  allow_implicit_invocation: false
