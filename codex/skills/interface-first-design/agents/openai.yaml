interface:
  display_name: "Interface First Design"
  short_description: "Optional workflow skill. Explicit invocation recommended."
  default_prompt: "Use $interface-first-design for this task."
policy:
  allow_implicit_invocation: false
