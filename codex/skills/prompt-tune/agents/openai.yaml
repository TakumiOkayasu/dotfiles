interface:
  display_name: "Prompt Tune"
  short_description: "Optional workflow skill. Explicit invocation recommended."
  default_prompt: "Use $prompt-tune for this task."
policy:
  allow_implicit_invocation: false
