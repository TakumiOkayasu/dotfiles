interface:
  display_name: "Orchestrate"
  short_description: "Optional workflow skill. Explicit invocation recommended."
  default_prompt: "Use $orchestrate for this task."
policy:
  allow_implicit_invocation: false
