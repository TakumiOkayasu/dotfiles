interface:
  display_name: "Bug Hunt"
  short_description: "Optional workflow skill. Explicit invocation recommended."
  default_prompt: "Use $bug-hunt for this task."
policy:
  allow_implicit_invocation: false
