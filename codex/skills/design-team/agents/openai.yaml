interface:
  display_name: "Design Team"
  short_description: "Optional workflow skill. Explicit invocation recommended."
  default_prompt: "Use $design-team for this task."
policy:
  allow_implicit_invocation: false
