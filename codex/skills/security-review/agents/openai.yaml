interface:
  display_name: "Security Review"
  short_description: "Security-only review for auth, input, secrets, injection, SSRF, XSS, traversal."
  default_prompt: "Use $security-review for this task."
policy:
  allow_implicit_invocation: true
