interface:
  display_name: "Instruction Surface Audit"
  short_description: "Optional workflow skill. Explicit invocation recommended."
  default_prompt: "Use $instruction-surface-audit for this task."
policy:
  allow_implicit_invocation: false
