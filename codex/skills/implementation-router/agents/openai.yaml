interface:
  display_name: "Implementation Router"
  short_description: "Classify implementation risk and route skills."
  default_prompt: "Use $implementation-router for this task."
policy:
  allow_implicit_invocation: true
