interface:
  display_name: "Measure"
  short_description: "Optional workflow skill. Explicit invocation recommended."
  default_prompt: "Use $measure for this task."
policy:
  allow_implicit_invocation: false
