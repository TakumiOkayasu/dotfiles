---
name: commit
summary: commit-msg の alias。commit は実行しない
profile: read-only
skills:
  - codex-handoff
---

# commit

$ARGUMENTS

`/prompt:commit-msg` と同じ扱いにする。ステージ済み差分から commit message 案だけを作る。`git commit` は実行しない。
