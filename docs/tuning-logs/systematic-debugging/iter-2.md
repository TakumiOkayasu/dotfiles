# iter 2 — systematic-debugging（B-1 数の不整合 を SKILL.md で整合化）

## 変更点（前回差分）

iter 1 で構造的穴と判定した **B-1（トリガー L14「2回以上失敗」と 失敗パターン L75「3回以上失敗→アーキ疑い」の数の不整合）** を SKILL.md で整合化。

### 具体修正

**L14 トリガー条件**:

```diff
- 修正を試みたが直らないとき（2回以上失敗）
+ 修正を試みたが直らないとき（1回以上失敗）
```

トリガーは「スキル発動タイミング」なので数を下げ、1回失敗でも発動させるように緩和。L75 と役割分離。

**L73-75 失敗パターン** (1行 → 4行):

```diff
-**3回以上の修正失敗 → アーキテクチャを疑う。ユーザーと相談。**
+修正が繰り返し失敗した場合、以下の段階で対応する:
+
+- **2回失敗**: Phase 1（再現）からやり直す。最小再現ケースと境界トレースを再検証。前回の根本原因仮説が誤っていた前提で再分析
+- **3回以上失敗**: アーキテクチャを疑う。ユーザーと相談（責務境界・層分離・データフローの構造的問題の可能性）
```

**意図**:
- L14 = スキル発動タイミング（「1回以上失敗」= 1回でも発動）
- L75 = スキル発動後のフェーズ戦略段階化（2回 = Phase 1 再実施 / 3回以上 = アーキ疑い）
- 数詞の矛盾を構造的に排除（トリガーと失敗パターンの役割分離）

SKILL.md 行数 110 → 113 (+3行、tdd iter 2 +15行より抑制)。

## 実行結果（シナリオ別）

| シナリオ | 成功/失敗 | 精度 | steps (tool_uses) | duration | retries |
|---|:---:|---:|---:|---:|---:|
| A（中央値: テスト失敗の原因特定） | ○ | 100% (7/7) | 0 | 35.2s | 0 |
| B（edge: 修正2回失敗後の再依頼） | ○ | 92% (5.5/6) | 0 | 39.0s | 0 |
| C（edge: 再現手順なし症状報告） | ○ | 100% (6/6) | 0 | 33.0s | 0 |

### 前回比（iter 1 → iter 2）

| 指標 | iter 1 | iter 2 | 変化 |
|---|---:|---:|---:|
| 精度 A | 100% | 100% | ±0pt ✓ |
| 精度 B | 92% | 92% | ±0pt ✓ |
| 精度 C | 100% | 100% | ±0pt ✓ |
| duration A | 57.2s | 35.2s | **-38%** |
| duration B | 49.4s | 39.0s | **-21%** |
| duration C | 40.0s | 33.0s | **-17.5%** |
| tool_uses 平均 | 1.0 | 0.0 | -100% |

**メトリクス観察**:
- [critical] 要件は全シナリオで ○（B の critical 2件も iter 1 に続き ○）
- duration 平均 48.9s → 35.7s で **-27%** の大幅改善
- tool_uses が全シナリオ 1 → 0 に揃う = subagent が外部参照なしで応答を組み立てた（自己完結性向上）
- SKILL.md +3行で duration 短縮 = tdd iter 2 の +56% とは逆方向。失敗パターンの段階化が「次アクションの迷い」を構造的に除去した効果と解釈
- B の精度 92% は iter 1 と変わらず。△ の原因は要件6（failure-logging 連携）で iter 1 と同じ = B-1 修正は要件6には効かなかった（狙い通り、波及なし）

### B-1 解消の実地観察

iter 1 で subagent B が指摘した「数の不整合」は、iter 2 では **subagent 自身が段階化を正しく読み取り自然適用** した:

- 「なぜ 3 回目のパッチを出さないか」節で L75 新文言を引用:
  - 2回失敗 → Phase 1 からやり直す
  - 3回以上失敗 → アーキテクチャを疑う
- 「2 回失敗済み + 3 回目依頼」を **アーキ疑い段階** と迷いなく解釈（iter 1 では subagent 自身が「本シナリオは両条件を満たすと解釈したが明文化されていない」と構造的穴として報告していた）
- Phase 1 再検証の 4 項目、Phase 2 再実施、アーキ疑いの 4 構造分類（責務境界/層分離/データフロー/不変条件）を **独立節で整理**

iter 1 では「構造的穴」として浮上した B-1 が、iter 2 では「明文化された段階戦略」として消化されている。

## 不明瞭点（今回新出）

### シナリオA

- **A-1' (新出)**: 架空シナリオでの模擬実行（実ファイルが無い）規定が SKILL.md に無い → subagent は「[要確認]」マーカーと未確認箇所明示で補完
- **A-2' (新出)**: 「最小再現ケース」の粒度基準（`npm test -- user.service` だけで足りるか、テスト単体抽出まで必要か）が未定義

### シナリオB

- **B-1' (新出・iter 2 修正の副作用候補)**: 「2回失敗」と「3回以上失敗」の境界解釈（3回目依頼時点は「まだ2回失敗段階」か「3回目を出す直前 = アーキ疑い段階」か）の明文規定なし
  - ただし subagent は正しく解釈して [critical] ○ → **実害は出ていない（予防的明文化の候補）**
- **B-2 (iter 1 継続)**: 「ユーザーと相談」の相談フォーマット未規定 → subagent は A/B/C 選択肢として裁量提示
- **B-3 (iter 1 継続)**: failure-logging との連携手順が SKILL.md 本文に無い → **要件6 部分的 の直接原因**

### シナリオC

- **C-1' (新出)**: Phase 2 以降の先取り提示基準（ユーザー応答時点でどこまで先走って言及してよいか）が未定義 → subagent は「概要のみ列挙」で裁量補完
- **C-2' (新出)**: デバッグ記録テンプレートをこの応答時点で埋めるべきか（情報ゼロでも骨子を出すか）が不明瞭 → subagent は未使用で裁量補完
- **C-3 (iter 1 継続)**: 「100%再現」の判定回数・判定環境（本番 / staging / ローカル）未規定
- **C-4 (iter 1 継続)**: ユーザー聞き取りテンプレートが未提供 → subagent は A/B/C 表形式で独自構築

## 裁量補完（今回新出）

- A: 再現手順・環境・スタックトレース要確認項目を表で構造化、境界トレース層分類（テスト/Service/Repository/DTO）を Node.js 一般構成から推定
- B: 構造的疑いの 4 分類（責務境界/層分離/データフロー/不変条件）を独自構築、ユーザー相談を A/B/C 選択肢に落とし込み
- C: 確認項目を A（症状記録6項目）/ B（頻度切り分け6項目）/ C（環境情報5項目）の 3 パートに構造化、切り分け観点（連続保存・ダブルクリック・楽観ロック）を一般デバッグ知見で補完

## empirical-prompt-tuning 側の曖昧点

なし（subagent 起動契約どおり報告できている）

## iter 1 提案との照合

iter 1 提案「L14 を『1回以上失敗』or『繰り返し失敗』に緩和、L75 に段階化明記」:

- L14 緩和 → 「1回以上失敗」採用（trigger の感度を上げる方向）
- L75 段階化 → 2段階（2回 / 3回以上）に明記、各段階の行動を言語化
- **予想通りの効果**: B-1 は subagent の自然適用で解消、副作用は B-1'（境界解釈）だけで軽微

## 次の修正案（iter 3 の 1 テーマ）

**採用候補（優先順）**:

1. **B-3（failure-logging 連携手順の不在）** — **iter 1・iter 2 連続で要件6 △ の直接原因**。実害中で精度 92% の押し下げ要因。1-2行の言及追加（「同じ失敗を繰り返さないため failure-logging スキルで記録する」等）で済む最小修正
2. **B-1' (境界解釈の明文化)** — iter 2 修正の副作用候補。subagent は正解釈中だが予防的明文化で盤石化
3. **C-2' (デバッグ記録テンプレートを埋めるタイミング)** — 小修正で済む

**採用**: **B-3** を iter 3 で潰す

**理由**:
- iter 1 → iter 2 で **連続して要件6 △** の直接原因。[critical] ではないが精度 92% から 100% への押し上げ要因
- 「同じ失敗を繰り返さない」(禁止事項 L102) は既に SKILL.md 内に書かれているが、failure-logging スキルとの連携手順が分離されているため具体アクションが取れていない
- 修正は最小: 失敗パターン節または禁止事項節に 1-2 行の連携言及を追加するだけ
- B-1' は iter 2 で subagent 裁量カバー済み（実害なし）、iter 4+ に回す
- tdd iter 3 の論理（iter 2 副作用を潰す）とは異なり、ここでは **連続 △ 継続の実害要因を優先** する。副作用 B-1' が実害なしのため

**見送り（iter 4+ に回す）**:
- A-1', A-2'
- B-1', B-2
- C-1', C-2', C-3, C-4

## 収束判定

- **連続クリアカウント: 1/3**（iter 2 が「iter 1 baseline との前回比計算成立」初回、重要スキル目標 3 まで連続 2 回残り）
  - 精度 ±0pt ✓
  - 新規不明瞭点 5件（A-1', A-2', B-1', C-1', C-2'）→ **0件ではない**ため厳密には連続カウント未成立
  - duration 変動 -27% → 収束条件 ±15% を超える改善方向だが変動幅は大きい
  - tool_uses 変動 -100% → 自己完結性向上、構造的欠陥なし
- → 厳密解釈では連続クリア 0 のまま。ただし [critical] 全 ○ + 構造的穴 B-1 解消は質的改善
- iter 3 以降で新規不明瞭点 0 + メトリクス飽和に向かわせる

## hold-out

未実施（連続3クリア達成時に scenarios.md シナリオD を実施）

## 次アクション

- 本セッションはここで区切る
- 次セッション: iter 3 で B-3（failure-logging 連携）を SKILL.md に明文化 → 新規 subagent 3並列で再評価
