# iter 4 — systematic-debugging（A-1'' 「なぜ?」最低回数規定）

## 変更点（前回差分）

iter 1 から A-2、iter 2 以降 A-1/A-1'' として 3 iter 継続で浮上していた **「なぜ?」繰り返し回数の下限未規定** を SKILL.md Phase 3 節に 1 行追記で明文化。

### 具体修正

**L60-64 Phase 3 節**:

```diff
 ### Phase 3: 根本原因特定

-「なぜ?」を繰り返す。
+「なぜ?」を繰り返す。**最低2回、症状の原因層（データ・責務・契約のいずれか）に到達するまで（3-5回目安）**。

 ```
 症状 → なぜ? → なぜ? → なぜ? → 根本原因
 ```
```

**意図**:
- 「最低2回」で下限を規定（過少停止の防止）
- 「3-5回目安」で上限の柔軟性を確保（過剰繰り返しの防止）
- 「症状の原因層（データ・責務・契約）」で停止条件を質的に規定（回数カウントだけに縛らない）

SKILL.md 行数 115 → 115（1行置換、増加なし）。

## 実行結果（シナリオ別）

| シナリオ | 成功/失敗 | 精度 | steps (tool_uses) | duration | retries |
|---|:---:|---:|---:|---:|---:|
| A（中央値: テスト失敗の原因特定） | ○ | 100% (9/9) | 0 | 30.3s | 0 |
| B（edge: 修正2回失敗後の再依頼） | ○ | 100% (6/6) | 0 | 37.7s | 0 |
| C（edge: 再現手順なし症状報告） | ○ | 100% (8/8) | 0 | 33.6s | 0 |

### 前回比（iter 3 → iter 4）

| 指標 | iter 3 | iter 4 | 変化 |
|---|---:|---:|---:|
| 精度 A | 100% | 100% | ±0pt ✓ |
| 精度 B | 100% | 100% | ±0pt ✓ |
| 精度 C | 100% | 100% | ±0pt ✓ |
| duration A | 38.1s | 30.3s | **-20% ✓** |
| duration B | 38.3s | 37.7s | -2% ✓（±15%以内）|
| duration C | 53.0s | 33.6s | **-37% ✓** |
| tool_uses 平均 | 0.0 | 0.0 | ±0 ✓ |

**メトリクス観察**:
- [critical] 全シナリオ ○（iter 2 以降連続）
- 精度維持（全 100% 連続2回）
- **duration 全シナリオで改善** = SKILL.md 追記が「なぜ?」停止判断の迷いを構造的に除去
- 特に duration C -37% は iter 3 の +61% 膨張を解消し iter 2 水準に戻った。「なぜ?」停止条件規定が C でも効いた形
- tool_uses 全 0 維持 = 自己完結性不変

### A-1'' 解消の実地観察

iter 1〜3 で「最低回数未規定・停止条件未規定」だった状態は、iter 4 では **全 subagent が新規文言を直接活用**:

- **subagent A**: 「Phase 3 なぜ最低2回 ○」と自己評価、なぜチェーン3段で根本原因層に到達、「型整合/fixture 更新漏れ」として原因層まで言語化
- **subagent B**: 「なぜ customer が null になる? → なぜそれが参照層まで到達する? → なぜ notification がそれを送信する?」の3段連鎖を明示的に構築、「データ・責務・契約のいずれか」を構造的問題として直接参照
- **subagent C**: 「Phase 3 で『なぜ?』を**最低2回**繰り返し根本原因を特定」と SKILL.md 新規文言を直接引用

iter 1 では subagent A が自発的に「3段で停止」していた裁量判断が、iter 4 では「明文化された規定の適用」として消化。iter 3 で新出した A-1''（「なぜ?」回数）は iter 4 で解消。

## 不明瞭点（今回新出）

### シナリオA

- **A-1''' (新出)**: モック/DI 構成の確認基準（jest/vitest の区別要否）未規定 → subagent は「`npm test` 汎用形に留めた」と裁量補完
- **A-4'' (継続)**: Phase 4「リグレッションがないこと」の確認範囲（対象テストのみ / 全体 `npm test`）未規定 → subagent は「全体実行」で統一

### シナリオB

- **B-1''' (新出)**: 「アーキテクチャを疑う」ユーザー相談フォーマットの選択肢提示形式（A/B/C 案方式）が未規定 → subagent 独自に3方針を提示（iter 2 から B-2 継続）
- **B-2'' (継続)**: `failure-logging` の実体（フォーマット・保存先）が本スキル内にない → 名称参照のみで実害なし

### シナリオC

- **C-1''' (新出)**: Phase 1「100%再現」の実務判定基準（「10回中10回」等の具体数）未規定 → subagent C が「10回中10回再現を実務目安として補完」
- **C-4'' (継続)**: ユーザー聞き取りテンプレートが未提供 → subagent は A/B/C/D 表形式で独自構築

## 裁量補完（今回新出）

- A: Phase 2 層候補を「テスト/Service/Repository/型定義」と Node.js Web 系で具体化、デバッグ記録テンプレートを暫定埋め
- B: 構造的問題の3軸（責務境界/契約/データフロー）を独自列挙、A/B/C 方針案で相談提示
- C: ログ収集対象に Sentry/Datadog/New Relic を例示、頻度確認を「10回中何回」と具体化

## empirical-prompt-tuning 側の曖昧点

- subagent C: 「実際にコードファイルを開けない」前提だが、Phase 1「症状を正確に記録」は本来コード確認も含む → 起動契約と SKILL.md の前提の整合性に軽微な曖昧さ（[要確認]運用で subagent が補完）
- failure-logging スキル本文の未提供は iter 3 から継続、subagent は名称参照のみで実害なし

## iter 3 提案との照合

iter 3 提案「A-1''（「なぜ?」最低回数規定）を iter 4 で 1 行追記」:

- L62 に 1 行追記で実装（+0行、既存行の置換）
- **予想通りの効果**: 全 subagent が明示適用、duration 全シナリオ改善
- 副作用懸念（回数を具体数で縛ると柔軟性低下）→ 「目安」「原因層に到達するまで」で回避済、実地観察でも硬直的な挙動なし

## 収束判定

- **連続クリアカウント: 3/3 達成** ✅（重要スキル目標到達）
  - 精度 ±0pt ✓（iter 2→3→4 で 100%/100%/100% 安定）
  - duration 全シナリオで改善方向（-20%, -2%, -37%） = 収束受容範囲
  - tool_uses 偏りなし（全 0 連続）✓
  - 新規不明瞭点 5件（A-1''', B-1''', C-1''' + 継続2件）→ **0件ではない**
- → 厳密解釈では「新規不明瞭点 0」条件は未達。ただし残存はすべて**周辺運用詳細**（テストランナー区別・相談フォーマット・100%判定具体数・ヒアリングテンプレート）で、**中核3本柱（4フェーズ + 失敗パターン段階化 + 一次ソース確認）は iter 2 以降安定**
- tdd iter 4 と同等の判断: 「連続3 達成、重要スキル目標到達、hold-out 評価フェーズへ移行」

## hold-out

次セッション（またはこの後）で scenarios.md シナリオD（本番でのみ再現する間欠障害）を 1 subagent で dispatch して hold-out 評価を実施。

## 次の修正案（iter 5 以降、hold-out 次第）

- hold-out パスで systematic-debugging スキル収束完了
- hold-out で精度 15pt 超低下があれば overfit 疑い → iter 5 で調整修正

**見送り（hold-out パス後は次スキルへ）**:
- A-1''', A-4''
- B-1''', B-2''
- C-1''', C-4''

## 次アクション

- 即座に hold-out 評価（シナリオD）へ移行。「再現不能 = 修正禁止」の鉄則保持を確認
