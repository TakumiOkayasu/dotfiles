# feature-pruning iter 4: B 由来 4 件解消

**実施日**: 2026-04-30
**対応ブランチ**: `chore/empirical-tuning-iter3-axis-format` (iter 3 と同ブランチで継続)
**修正テーマ**: iter 3 シナリオ B で識別された 5 論点のうち E.2 (ばらつきとの競合解決) を iter 3 内で解消、残 4 件 (B-1 / B-2 / B-3 / B-4) を本 iter で解消

## Context

iter 3 シナリオ B (15 機能 SaaS 売上ダッシュボード) で識別された skill 由来 5 論点:

1. **E.2 フッター F-Sub ばらつき ±2 + 他軸削除確定の競合** → iter 3 で繰り上げ解消済 (削減判定ルール「ばらつきとの競合解決」項追加)
2. **B-1 ソート 🔴 削除 vs 🔁 代替の境界 (ライブラリ採用前提時の判定優先順位)**
3. **B-2 複合機能の主要素特定ルール** (ヘッダー = ロゴ / ナビ / ユーザーメニュー、いずれが主要素か)
4. **B-3 複合機能の粒度ルール** (「KPI カード x 4」を 1 機能 / 4 機能のどちらで扱うか)
5. **B-4 「画面外代替」(F-Sub=4 規定) を Round 1 で引くべきか、各ラウンドの重点軸のみ採点かのルール**

iter 3 シナリオ A 評価 (受注管理 8 機能 / 削除確認ダイアログ 🔴 確定) と iter 3 シナリオ C 評価 (ログイン 5 機能 / Okta SSO 強制下) でも、複合機能 (パスワード入力 + 表示切替トグル / ログイン保持) で同型論点が再発し、本 iter での明文化を妥当と判断。

## 修正内容

### B-1: ライブラリ採用前提時の 🔴 / 🔁 境界 (削減判定ルール節)

`claude/skills/feature-pruning/SKILL.md` 「判定の使い分け」節 (L226 付近) 直後に新規ブロック追加:

- 同等機能を提供するライブラリ (テーブル / フォーム / 認証) 採用前提時、F-Sub=3 でも 🔁 を優先 (付随取得)
- ライブラリ未採用 / 判断未確定なら F-Nec ≤ 1 → 🔴 ルール通常適用
- 判定例 (ソート F-Nec=1 / F-Cnv=4 / F-Sub=3) を 2 通り併記: TanStack Table 採用前提 → 🔁、表自作 → 🔴
- 入力側でライブラリ採用前提の有無を明示 (前提不明時は subagent が「未採用」とみなして採点)

### B-2 / B-3 / B-4: 起動契約タスク 3-4 の補強

`claude/skills/feature-pruning/SKILL.md` 起動契約 (L143-148) 拡充:

**タスク 3 (採点軸の固定、B-4 解消)**:
- 各 Round で全機能 × 全 3 軸を採点 (Round 1 = F-Nec のみ等のルールではない)
- 手法 (YAGNI / Convention / Existing) は採点の重点ではなく検証視点
- 「画面外代替」(F-Sub=4) は Round 1 (YAGNI) でも採点対象、手法問わず F-Sub=4 該当機能は明示

**タスク 4 (複合機能採点ルール、B-2 / B-3 解消)**:
- **主要素特定ルール (B-2)**: 主要素 = 「その要素を消すと主要動線が成立しなくなる」要素。候補が複数なら F-Nec 最高の要素を採用、同点なら使用頻度の高い方。例 (ヘッダー → ナビが主要素 / パスワード入力 (表示切替付き) → パスワード入力本体が主要素)
- **粒度ルール (B-3)**: 機能リストは入力側で固定、subagent 側で分割 / 集約しない。例 1: 「KPI カード x 4」1 機能扱い → 1 機能採点 + 内訳粒度で過剰指摘。例 2: 「KPI カード A」「KPI カード B」… 4 機能扱い → 4 機能採点。粒度疑義時は親に確認を返す

## 動作テスト

iter 3 シナリオ A / C / hold-out で使われた前提を mock として、本 iter 修正後の skill が同シナリオを矛盾なく扱えるか親側ロジック確認 (新規 dispatch なし、修正テーマと skill 文言の整合のみ点検):

| 論点 | iter 3 で発生した曖昧さ | iter 4 修正後の skill 文言適用 | 結果 |
|---|---|---|---|
| B-1 ライブラリ前提 | iter 3 シナリオ B のソート (F-Nec=1 / F-Cnv=4 / F-Sub=3) で 🔴 vs 🔁 裁量 | 「TanStack Table 採用前提 → 🔁、表自作 → 🔴」を直接適用可能 | ✅ 解消 |
| B-2 主要素特定 | iter 3 シナリオ B のヘッダーで「ロゴ / ナビ / ユーザーメニュー」のどれが主要素か裁量 | 「F-Nec 最高 = ナビ、ロゴ / ユーザーメニュー / 通知バッジは内訳要素」と一意に決まる | ✅ 解消 |
| B-3 粒度 | iter 3 シナリオ B の「KPI カード x 4」を 1 機能 / 4 機能裁量 | 「入力側固定」「疑義時は親確認」で subagent 自由度を排除 | ✅ 解消 |
| B-4 全軸採点 | iter 3 シナリオ B で Round 1 (YAGNI) は F-Nec のみ採点する解釈が出る可能性 | 「全 Round × 全 3 軸」「画面外代替 (F-Sub=4) は Round 1 でも明示」を明記 | ✅ 解消 |

## 動作テストを subagent dispatch で行わない理由

- iter 3 で 4 シナリオ (B / A / C / hold-out D) を dispatch 済み、修正テーマ (A.1 / A.2 / E.1 / E.2) は全シナリオで活用確認済
- 本 iter は明文化 (新ルールの追加ではなく、既存運用ルールの skill 文言への昇格) のみ
- 結論ラベル / 採点値が変わる修正ではなく、subagent 自由度の制約 (粒度 / 主要素 / 全軸採点) なので、新規 dispatch コストに見合う効用が小さい
- 必要なら次回シナリオ (iter 5 候補不在のため未予定) で従属的に検証する

## 収束判定

- iter 3 シナリオ B 由来の 5 論点全てを解消 (E.2 = iter 3 内、B-1 〜 B-4 = iter 4)
- iter 3 シナリオ A / C / hold-out D は skill 由来不明瞭点 0 / 2 (D は対象方針側) で完結
- iter 4 残 候補なし (本 iter で B 由来全件消化)
- **判定: ✅ 収束**。次の修正候補は新シナリオ評価で出た時点で起票

## 次アクション

- [x] SKILL.md 修正 (B-1 / B-2 / B-3 / B-4 計 2 箇所 Edit)
- [x] iter-4.md 作成
- [ ] PROGRESS.md に iter 4 結果転記
- [ ] iter 3 + iter 4 を同一コミット or 別コミットにするかユーザー判断 (推奨: 1 コミット = `chore: empirical-tuning iter 3/4 (シナリオ A/C/hold-out + B 由来 4 件 + ばらつき競合解決)`)
