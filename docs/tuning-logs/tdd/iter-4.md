# iter 4 — tdd

iter 2/3 両シナリオC で残存していた C-3'（確度確認の定型テンプレ不在）を潰す。「出力形式テンプレート」セクション冒頭に確度確認テンプレートを追加。

## 変更点（前回差分）

`claude/skills/TDD/SKILL.md` 「出力形式テンプレート」セクションの先頭に「仕様確度の確認時」ブロックを追加（約23行）。内容:

- 発言シグナル引用 → AI の見立て → 確度×推奨アプローチ表 → 確認したい点（最小情報先取り）の4パートから成るテンプレート
- 末尾に「『探索的』は常にユーザー申告制。申告前に実装先行しない」を再掲

## 実行結果（シナリオ別）

| シナリオ | 成功/失敗 | 精度 | steps (tool_uses) | duration | retries | 前回比 |
|---|:---:|---:|---:|---:|---:|---|
| A（中央値: 純粋関数 TDD） | ○ | 100% (7/7) | 1 | 42.0s | 0 | 精度維持 / -42% |
| B（edge: レガシー/特性テスト） | ○ | 100% (6/6) | 1 | 45.3s | 0 | 精度維持 / -41% |
| C（edge: 曖昧仕様の適用判断） | ○ | 100% (5/5) | 1 | 37.1s | 0 | 精度維持 / -17% |

**メトリクス観察**:
- 全シナリオ精度100%維持
- tool_uses **全シナリオ 1 に揃う**（iter 3: 2/2/1 → iter 4: 1/1/1）。偏り3-5倍なし、自己完結性が iter 1 水準に回復
- duration 平均 **-36%（64.5s → 41.5s）**。テンプレ提供で subagent の出力生成が効率化された（SKILL.md 行数は増えたが迷いが減ったため全体として短縮）
- **シナリオB/C で iter 4 追加テンプレが実際に適用された**（両方とも「## 仕様確度の確認 📋」を自然に出力、シグナル引用→見立て→表→質問の4パート構造を忠実に踏襲）→ **C-3' の解消を実地観察**
- シナリオA は仕様=確定のため確度確認テンプレは発動せず（対象外）。代わりに iter 3 の表形式ガード化報告を 11件で適用

## 不明瞭点（今回新出・残存）

### シナリオA
- **A-1' (残存・iter 1-3 継続)**: 三角測量と明白な実装の境目（Cycle 1 仮実装→Cycle 2 明白な実装への切替基準）
- **A-new-1 (新出)**: テストリストの □ とガード化後の記法関係（完了/ガード済/別マーク）
- **A-new-2 (新出)**: `it.each` 等のパラメタライズドテストと「1サイクル=1テストケース」鉄則の関係
- **A-new-3 (新出)**: 「仕様書に明記のないエッジ」（前後空白、大文字小文字、TZ付き等）にどこまで踏み込むか

### シナリオB
- **B-new-1 (新出)**: 特性テスト分離4選択肢のうちプロジェクト慣習がない場合の fallback 推奨
- **B-4' (残存・iter 1-3 継続)**: 特性テスト→通常テスト「昇格」の具体手順（マーカー剥がし / TODO 除去 / アサーション見直し）
- **B-new-2 (新出)**: 「テスタブルにする」リファクタの粒度・技法名（Seam, ゴールデンマスター等）への言及不足
- **B-new-3 (新出)**: 現状振る舞いの「取得方法」（REPL 実行、スナップショットテスト等）未言及
- **B-new-4 (新出)**: マトリクス「不明 × テスタブルでない」3段階手順内のユーザー確認タイミング

### シナリオC
- **C-1 残存・iter 1 C-2**: 「〜な感じで」と「詳細は作りながら決める」が同時発話時のシグナル強度優先ルール
- **C-new-1 (新出)**: ステップ0（検出）とステップ1（適用判断）の質問統合ルール（最初の質問N件まで等）
- **C-new-2 (新出)**: 「方針のみ」先取り質問のドメイン別例示（CRUD系 / 計算系 / I/O系）

## 裁量補完（今回新出）

- A: テストリスト作成後のユーザー確認スキップ（Auto mode）、仮実装→明白な実装の切替判断、ガード化の11件一括表形式
- B: pytest マーカー `@pytest.mark.characterization` の独断採用、DB fixture `tmp_path` 仮置き、DI 化リファクタ粒度、仕様確度確認とテストリスト確認を1回にまとめる判断
- C: 見立ての併記順（「方針のみ（探索的の可能性）」）、先取り質問3件の具体化（保存先/最小機能/履歴単位）、ステップ0 をステップ1 確定後に回す順序判断

## empirical-prompt-tuning 側の曖昧点

なし（subagent 起動契約どおり報告できている）

## iter 3 → iter 4 の構造的変化

| 指標 | iter 3 | iter 4 | 評価 |
|---|---|---|---|
| 精度 | 100/100/100 | 100/100/100 | 維持 |
| tool_uses | 2/2/1 | **1/1/1** | **iter 1 水準に回復** |
| duration 平均 | 64.5s | 41.5s | **-36% 大幅改善** |
| 新規不明瞭点 | 7件 + 再出7件 | 9件 + 再出3件 | 再出比率低下 |
| iter N 修正の解消 | A-2' | **C-3' 解消（B/C 両方で実地適用）** | ✓ |
| 副作用（新たな穴） | なし | なし | 連続2回なし |

**解釈**:
- iter 4 の修正は **目的（C-3'）を完全に解消**し、副作用なし
- tool_uses の iter 1 水準回復と duration -36% は、テンプレ提供が **subagent の「何を出力すべきか」の迷いを構造的に除去**した証拠
- 新出9件の多くは「より深層の運用詳細」（パラメタライズド、Seam 技法名、ドメイン別例示）で、スキルのコア責務からは外れる周辺事項

## 収束判定

- **連続クリアカウント: 3/3 達成** ✅
  - iter 2: A-2（即 PASS ガード）解消
  - iter 3: A-2'（ガード連続時の報告形式）解消、副作用なし
  - iter 4: C-3'（確度確認テンプレ）解消、副作用なし
- 重要スキル目標（連続3）達成 → **hold-out 評価（シナリオD）へ進む**
- 残存不明瞭点は存在するが、すべて「周辺の運用詳細」であり、中核のRED-GREEN-REFACTOR / 判断マトリクス / 特性テスト経路の3本柱はいずれも安定適用されている

## hold-out

**次セッションで実施予定**（scenarios.md シナリオD = 既存テスト期待値の疑わしい書き換え依頼）

- 焦点: 鉄則2「テストは仕様。期待する振る舞いをユーザー確認なしに変えない」「振る舞いが変わっていないのにアサーションの期待値を変える」禁止の自然適用
- hold-out 成功時 → **tdd スキル収束完了**、Phase 2 次スキル（systematic-debugging）へ
- hold-out 失敗時 → iter 5 で修正、連続カウントリセット

## 次アクション

- 本セッションはここで区切る
- 次セッション: hold-out 評価（シナリオD 1本を新規 subagent で dispatch）
- hold-out 成功なら Phase 2 systematic-debugging に着手
