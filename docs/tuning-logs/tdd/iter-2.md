# iter 2 — tdd

iter 1 で採用した A-2（既に一般化済みの実装で後発テストが即 PASS する扱い）に対処する節を SKILL.md に追加し、新規 subagent 3並列で再評価。

## 変更点（前回差分）

`claude/skills/TDD/SKILL.md` ステップ3「RED」末尾に `#### 例外: サイクルを重ねた結果、追加テストが即PASSする場合` を追記（約15行）。内容:

- 実装は追加しない（既に一般化済み）
- テストは回帰ガードとして残す
- RED 確認不要、鉄則1違反にならない
- コメントで明示（例: `// covered by generalization at cycle N — kept as regression guard`）
- 判断基準: 削除しても振る舞いが変わらない → ガード、変わる → 一般化不完全 → 通常サイクルに戻る

## 実行結果（シナリオ別）

| シナリオ | 成功/失敗 | 精度 | steps (tool_uses) | duration | retries | 前回比 |
|---|:---:|---:|---:|---:|---:|---|
| A（中央値: 純粋関数 TDD） | ○ | 100% (7/7) | 2 | 61.5s | 0 | 精度維持 / 時間 +78% |
| B（edge: レガシー/特性テスト） | ○ | 100% (6/6) | 2 | 49.9s | 0 | 精度維持 / 時間 +46% |
| C（edge: 曖昧仕様の適用判断） | ○ | 100% (5/5) | 1 | 42.5s | 0 | 精度維持 / 時間 +43% |

**メトリクス観察**:
- 全シナリオ精度100%維持（[critical] 含む全要件 ○）
- tool_uses は A/B が 1→2、C は 1 据え置き。偏り判定（3-5倍）は依然成立せず自己完結性 OK
- **duration が全シナリオで40-80%増**。SKILL.md の追記（約15行）によるプロセス説明増 + 仮想遂行の出力量増が要因。iter 3 以降で SKILL.md 追記時は冗長化を抑制すべきシグナル
- **シナリオA で iter 2 追加節が実際に適用された**（サイクル3「"2026-02-30" → false」が正規表現と Date ロールオーバで即 PASS → ガード扱いに分岐）→ A-2 の解消を実地で観察

## 不明瞭点（今回新出・残存）

### シナリオA
- **A-2' (新出・iter 2 修正の波及)**: 即 PASS ガードが**連続**発生するケース（サイクル3-10 が全てガード化）の**報告形式**が未規定。1件ずつ「例外適用」を報告するか、まとめて表化するか
- **A-1' (残存)**: 「三角測量 → 明白な実装」の移行境界ルール未記述（iter 1 A-1 と同根）
- **A-3' (新出)**: プロジェクト検出失敗時の粒度。「1つでも検出不能なら確認」が厳しすぎ、軽微な未検出（既存テスト配置慣習など）でも確認必須に読める
- **A-4' (新出)**: Auto モード等での「テストリスト確認」「最初のRED-GREEN後の方向性確認」の省略可否

### シナリオB
- **B-1' (新出)**: 特性テスト段階での REFACTOR 可否（フェーズ1で DI 化してよいか、フェーズ2開始後か）の順序
- **B-2' (新出)**: 外部依存の「現状値採取」手順（本番DB照会可否、ローカル実行採取など）の指針なし
- **B-3' (新出)**: TODO コメント書式が `//` 固定で書かれ、Python では `#` に置換する必要がある
- **B-4' (残存・iter 1 B-1 相当)**: 特性テスト昇格の具体オペレーション（ファイル移動/タグ除去）未定義、昇格判定基準も未記述
- **B-5' (新出)**: 仕様確度「不明」時のユーザー確認タイミング（ステップ1/2/4 のどこで投げるか）

### シナリオC
- **C-2' (残存・iter 1 C-2 相当)**: 「方針のみ」と「探索的」の境界（「〜な感じで」「作りながら決める」はどちらにも部分一致）
- **C-3' (新出)**: 判断マトリクスの「**ユーザーに確認**」の具体文言テンプレートが「ユーザー確認ポイント」表に不在
- **C-4' (新出)**: ステップ0「最初に必ず実行」とステップ1「適用判断」の前後関係。確度未定で適用しない可能性がある場合のステップ0の意味が薄い
- **C-5' (新出)**: ユーザー申告が曖昧に返った場合（「両方の中間かも」）の再質問ポリシー未定義

## 裁量補完（今回新出）

- A: テストファイルパス `src/isValidISODate.test.ts`、Auto モード推定での確認省略、RED 実行出力の文言再現、ブランチ名仮置き
- B: 特性テストディレクトリ `tests/characterization/`、DB モック戦略（monkeypatch → DI 化の順）、現状値の採取前提
- C: 「方針のみ / 探索的」の2択提示（4分類から絞り込み）、入出力イメージの追加質問、ステップ0 の後回し判断

## empirical-prompt-tuning 側の曖昧点

なし（subagent 起動契約どおり報告できている）

## iter 1 → iter 2 の構造的変化

| 指標 | iter 1 | iter 2 | 評価 |
|---|---|---|---|
| 精度 | 100% / 100% / 100% | 100% / 100% / 100% | 維持 |
| tool_uses 偏り | 1/1/1 | 2/2/1 | 偏り3-5倍は未発生（OK） |
| duration 平均 | 32.8s | 51.3s | +56%（SKILL.md 追記影響） |
| 新規不明瞭点 | 6件 | 12件（うち4件は iter 1 と同根の再出） | 増加 |
| iter 1 指摘の解消 | — | **A-2 解消（実地観察）** | ✓ |
| 副作用（新たな穴） | — | A-2' 1件（報告形式のみ） | 軽微 |

**解釈**:
- iter 2 の修正は **目的（A-2）を完全に解消**。波及は A-2'（報告形式）1件のみで軽微
- 新出12件のうち 4件（A-1'・B-4'・C-2'）は iter 1 と同根 → iter 1 の判断「iter 3+ に後回し」が**正しい順序**
- 残る8件は iter 1 では観察されなかった「より細かい」曖昧点 → **精度100%到達後に初めて炙り出される深層の穴**（empirical-prompt-tuning「裁量補完の解像度上昇」パターン）

## 次の修正案（iter 3 の 1 テーマ）

**採用候補（優先順）**:

1. **A-2'（即PASSガード連続時の報告形式）** — iter 2 修正の直接の副作用で整合性が取れる。1-2行追記で済む
2. **B-3'（TODO コメント書式）** — 機械的修正（`//` を「コメント」に抽象化するか言語別表記）
3. **C-2'（方針のみ/探索的境界）** — iter 1 から残存する構造的曖昧。ただし判断マトリクス拡張で 1 テーマ超過の懸念あり（iter 1 の見送り判断）

**採用**: **A-2'** を iter 3 で潰す

**理由**:
- iter 2 の変更と直接接続するテーマで、読み手にとって修正の因果が明瞭
- 修正は小規模（「連続発生時は表形式でまとめてよい」を1-2行追記）
- B-3' や他の新出を **iter 4 以降のバッチ候補**として温存しつつ、iter 3 で「連続1クリア」を確保しやすい

**見送り（iter 4+ に回す）**:
- A-1'（三角測量移行境界）、A-3'（検出粒度）、A-4'（確認省略可否）
- B-1'、B-2'、B-4'、B-5'
- C-2'（判断マトリクス拡張が必要）、C-3'、C-4'、C-5'

## 収束判定

- **連続クリアカウント: 1**（iter 2 で前回比 A-2 解消を確認 → カウント開始）
- 重要スキルのため連続3目標 → **iter 5 まで継続**（iter 3: A-2' / iter 4: 1 テーマ / iter 5: 1 テーマ）
- duration 増加傾向に注意。SKILL.md 行数インフレを iter 3 以降で監視

## hold-out

未実施（収束判定（連続3クリア）到達時に実施、scenarios.md シナリオD）

## 次アクション

- 本セッションはここで区切る
- 次セッション: iter 3 で A-2'（即PASSガード連続時の報告形式）を SKILL.md に 1-2 行追記 → 新規 subagent 3並列で再評価
