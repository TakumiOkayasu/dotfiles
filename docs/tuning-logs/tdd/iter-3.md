# iter 3 — tdd

iter 2 で採用した A-2 修正の波及として新出した A-2'（即 PASS ガードが連続発生する際の報告形式）を潰す。新規 subagent 3並列で再評価。

## 変更点（前回差分）

`claude/skills/tdd/SKILL.md` ステップ3「RED」の例外節末尾に1行追加:

```
**連続してガード化が発生する場合の報告**: 3件以上が連続して即PASSするなら、
個別のサイクル報告を繰り返さず1つの表にまとめてよい（列: ケース / 即PASSの
理由 / ガードコメント）。最後にまとめて「N件ガード化、新規実装なし」と1行で
完了報告する。
```

## 実行結果（シナリオ別）

| シナリオ | 成功/失敗 | 精度 | steps (tool_uses) | duration | retries | 前回比 |
|---|:---:|---:|---:|---:|---:|---|
| A（中央値: 純粋関数 TDD） | ○ | 100% (7/7) | 2 | 72.1s | 0 | 精度維持 / +17% |
| B（edge: レガシー/特性テスト） | ○ | 100% (6/6) | 2 | 77.0s | 0 | 精度維持 / +54% |
| C（edge: 曖昧仕様の適用判断） | ○ | 100% (5/5) | 1 | 44.5s | 0 | 精度維持 / +5% |

**メトリクス観察**:
- 全シナリオ精度100%維持
- tool_uses 偏り：iter 2 と同一（2/2/1）、3-5倍の偏りなし
- **シナリオA で iter 3 追加節が実際に適用された**。12件のガード化を表形式で1つにまとめ、「12件ガード化、新規実装なし」と1行完了報告 → **A-2' の解消を実地観察**
- シナリオB は特性テスト経路のためガード化発生なし（iter 3 修正の対象外）、シナリオC は適用判断で停止（対象外）
- duration は B で +54%（subagent 応答の冗長度は個体差の範囲、tool_uses 同一で品質は維持）

## 不明瞭点（今回新出・残存）

### シナリオA
- **A-1' (残存・iter 1/2 から継続)**: 「三角測量 → 明白な実装」の移行境界。空文字→false 追加時に一気に正規表現まで飛ぶのが「仮実装の段階的一般化」か「明白な実装」かの区別が曖昧
- **A-3' (残存・iter 2)**: プロジェクト検出の厳格さ。既存テスト閲覧前提か推測可かが読み取れない
- **A-4' (残存・iter 2)**: Auto mode 等での「テストリスト確認」「方向性確認」の省略可否
- **A-5' (新出)**: ガード化コメントの日本語プロジェクトでの記法（英語例のみ）

### シナリオB
- **B-1' (残存・iter 2)**: 特性テスト段階でのリファクタ可否、および DBスタブ手法（monkeypatch / fakes / Repository DI）の推奨順序
- **B-3' (残存・iter 2)**: TODO コメント書式の言語別対応
- **B-4' (残存・iter 2 / iter 1 B-1)**: 特性テスト昇格の具体オペレーションと判定基準
- **B-6' (新出)**: 「TDDで進めて」と指示されたのに特性テスト経路に切り替える場合の承認フロー
- **B-7' (新出)**: 特性テストの網羅範囲（分岐全て / 代表入力のみ）
- **B-8' (新出)**: 判断マトリクスで「不明×テスタブルでない」（最重ケース）の手順が1行と薄い

### シナリオC
- **C-3' (残存・iter 2)**: 確度確認の「定型テンプレ」が「出力形式テンプレート」セクションに不在
- **C-4' (残存・iter 2)**: ステップ0 未検出 × ステップ1 曖昧 の合流ルール
- **C-6' (新出)**: 「方針のみ」の統合テスト粒度と E2E 除外条件との線引き
- **C-7' (新出)**: 「探索的」2択（(a) 実装先行 / (b) 仕様先行）の推奨デフォルト・比較

## 裁量補完（今回新出）

- A: テストファイル配置・命名規約、ユーザー確認スキップ、ガード化表形式の適用（iter 3 修正に沿って自然実行）、REFACTOR スキップ判断
- B: pytest 仮定、分離方式（`tests/characterization/` + `pytest.mark.characterization` の2段）、DB 仮包み手法 monkeypatch、特性テスト期待値の仮置き、特性テスト導入宣言の事前確認追加
- C: ステップ0 と ステップ1 の確認を1メッセージに束ねる、「方針のみ〜探索的の境界」という中間表現、追加質問（保存先・履歴単位）

## empirical-prompt-tuning 側の曖昧点

なし（subagent 起動契約どおり報告できている）

## iter 2 → iter 3 の構造的変化

| 指標 | iter 2 | iter 3 | 評価 |
|---|---|---|---|
| 精度 | 100/100/100 | 100/100/100 | 維持 |
| tool_uses | 2/2/1 | 2/2/1 | 同一（偏り無し） |
| duration 平均 | 51.3s | 64.5s | +26%（iter 2 +56% の半分に鈍化） |
| 新規不明瞭点 | 12件（うち4件再出） | 14件（うち7件再出） | 再出比率が上昇 |
| iter N 修正の解消 | A-2 | **A-2' 解消（実地観察）** | ✓ |
| 副作用（新たな穴） | A-2' 1件 | なし | 良化 |

**解釈**:
- iter 3 の修正は **目的（A-2'）を完全に解消**し、**副作用なし**（iter 2 より良化）
- 新出 7件の多くは iter 2 で既出の不明瞭点が別シナリオで再出現した形 → 「スキル全体の解像度が上がり、同じ構造的曖昧が複数シナリオで炙り出される」パターン
- 残存 7件のうち **A-1', B-4', C-3' は iter 1/2 から 2-3 iter 継続** → 次以降の優先候補
- duration 増加率は iter 2 の +56% から +26% に鈍化 → SKILL.md 追記を1行に抑えた効果が出ている

## 次の修正案（iter 4 の 1 テーマ）

**採用候補（優先順）**:

1. **C-3'（確度確認の定型テンプレ不在）** — 「出力形式テンプレート」セクションに追加するだけで済む最小修正。iter 2 から残存で因果が明瞭
2. **A-1'（三角測量 → 明白な実装 の移行境界）** — iter 1 から 3 iter 継続の構造的曖昧だが、既存の GREEN テクニック表に情報はあり、「運用判断の記述不足」にとどまる
3. **B-3'（TODO コメント書式の言語対応）** — 機械的修正、波及性低い

**採用**: **C-3'** を iter 4 で潰す

**理由**:
- SKILL.md 既存セクション「出力形式テンプレート」に確度確認テンプレを追加するだけ（4-6行想定）で、冗長化リスク小
- iter 2 シナリオC、iter 3 シナリオC の両方で観察された残存曖昧で、修正の因果が明瞭
- 連続3クリア目標の iter 4 に最適な「小さく確実な1テーマ」

**見送り（iter 5+ に回す）**:
- A-1'（構造的だが既存テクニック表で部分カバー済み）
- A-3', A-4', A-5'
- B-1', B-3', B-4', B-6', B-7', B-8'
- C-4', C-6', C-7'

## 収束判定

- **連続クリアカウント: 2**（iter 2 で A-2 解消 / iter 3 で A-2' 解消、副作用なし）
- 重要スキル目標: 連続3 → **iter 4 で連続3クリア達成なら hold-out シナリオD 評価へ進む**
- tool_uses 偏り・精度・副作用すべて安定。収束に近い兆候

## hold-out

未実施（次 iter で連続3クリア達成時に実施、scenarios.md シナリオD）

## 次アクション

- 本セッションはここで区切る
- 次セッション: iter 4 で C-3'（確度確認の定型テンプレ）を「出力形式テンプレート」セクションに追加 → 新規 subagent 3並列で再評価。**連続3クリア達成なら続けて hold-out 評価**
