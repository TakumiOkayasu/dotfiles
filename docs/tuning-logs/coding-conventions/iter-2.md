# coding-conventions iter 2: L149 try/catch 配置判定追記 → 3 並列再評価

**実施日**: 2026-04-24
**rule 変更**: L149 に 1 行追記 (try/catch 配置の判定基準)

## 修正内容

**修正前 (L149)**:
```
| エラー伝播 | `try/catch` で捕捉するか、呼出側に任せる (握り潰し禁止) |
```

**修正後 (L149)**:
```
| エラー伝播 | `try/catch` で捕捉するか、呼出側に任せる (握り潰し禁止)。**判定**: 関数が意味のある回復・文脈付与・ログ出力を行えるなら捕捉、できないなら上位へ伝播 |
```

## 実行結果 (シナリオ別)

| シナリオ | 成功/失敗 | 精度 | tool_uses | duration | retries |
|---|:---:|:---:|:---:|---:|:---:|
| A (複合違反) | ○ | 8/9 (critical 5/5) | 1 | 33.0s | 0 |
| B (React 命名例外) | ○ | 6/6 (critical 3/3) | 1 | 23.1s | 0 |
| C (async+error+log) | ○ | 8/8 (critical 5/5) | 1 | 30.9s | 0 |
| **平均** | **○** | **100% critical** | **1** | **29.0s** | **0** |

全 [critical] 達成、tool_uses 全 1、平均 duration 29s (iter 1 比 -15%)。**連続 2/2 達成** ✅。

## CC-1-C-1 解消確認 (iter 2 テーマ)

シナリオ C subagent が**新文言を直接引用**して try/catch 配置を判断:

> **try/catch 配置判断 (rule L149 引用)**
>
> > **判定**: 関数が意味のある回復・文脈付与・ログ出力を行えるなら捕捉、できないなら上位へ伝播
>
> → `syncUsers` は「同期処理」という文脈を持つため、**関数内で捕捉しログ出力 (文脈付与) + 上位へ再 throw** が適切。

iter 1 では「低レイヤ同期処理と推定して呼出側伝播」と裁量補完だったが、iter 2 では新文言を根拠に「文脈付与があるため関数内捕捉」と**具体的判断**に転換。CC-1-C-1 が構造的に解消。副作用としてシナリオ C の修正方針 (呼出側伝播 → 関数内 try/catch+ログ+再 throw) が変化したが、いずれも rule 準拠。

## 不明瞭点 (今回新出 / 再出)

### CC-2-A-1: L106 未取得 vs 空結果 (CC-1-A-1 再出、iter 3 候補)

subagent A: 「L106 『未取得』と『空結果』を区別する必要がある場合は null 許容 — 本関数の元実装は `data === null` と `length === 0` を区別していた可能性があるが、シナリオに仕様記載なし。空配列返却に統一した (区別不要と仮定)。」

iter 1 の CC-1-A-1 と同じ論点。2 iter 連続浮上。

### CC-2-A-2: L211 `execute*` 例外と `parse*` の扱い (軽微、新出)

subagent A: 「L211 `execute*` 例外と `parseData` の扱い。rule は `process*` を曖昧としているが `parse*` は例示にない。`parseData` は動詞 + 目的語があり許容範囲と判断。」→ 命名例外リストの網羅性に関する論点、critical ではない。

### CC-2-B-1: L209 React 例外範囲 (CC-1-B-1 のバリエーション、iter 3 候補)

subagent B: 「L209 の『React 例外』はイベントハンドラ (`onClick` 等にバインドする関数) に限定されるのか、コンポーネント内ローカル関数全般かは断言されていない。ここでは DOM イベント受信関数に限定と解釈した。」

iter 1 の CC-1-B-1 と同じ論点を別角度 (本件は「コンポーネント内ローカル関数全般か」)。2 iter 連続浮上。

### CC-2-C-1: L149 「意味のある回復」判定粒度 (軽微、新出)

subagent C: 「L149 の『意味のある回復』判定の粒度 — `syncUsers` は回復手段を持たないが『文脈付与+ログ』で捕捉可と解釈。回復不能時は上位伝播のみで足りるケースとの境界は裁量。」

新文言自体は機能 (CC-1-C-1 解消) しつつ、さらに粒度の細分化を subagent が要望。critical には響かない。

## 裁量補完 (今回新出)

- subagent A: `parseData` → `filterTypeARecords` への改名提案 (命名軽微論点)
- subagent B: `orderHandlers.ts` ファイル名の改名指摘は省略 (スコープ外)、`handleSubmit` の FormEvent 判定
- subagent C: `logger` の import、`source.name` / `target.name` プロパティ仮定 (`[要確認]` で補完)

## 分析

### iter 3 テーマ選定の優先度

2 iter 連続で浮上した論点を比較:

| 候補 | iter 1 | iter 2 | 影響度 | 修正容易度 |
|---|:---:|:---:|:---:|:---:|
| **CC-1-B-1/CC-2-B-1 (L209 React 例外境界)** | ○ | ○ | 高 (React プロジェクト頻出) | 1 行追記 |
| CC-1-A-1/CC-2-A-1 (L106 null 許容) | ○ | ○ | 中 (返却設計限定) | 1 行追記 |

**選定**: **CC-1-B-1 / CC-2-B-1** を iter 3 で解消。理由:
1. 2 iter 連続で subagent の独立解釈が必要になる論点 (「イベント受信限定」vs「コンポーネント内全般」)
2. React は汎用的なフレームワーク、実害範囲広
3. 1 行追記で判定基準を明確化できる

## iter 3 修正案

**対象**: L208-209 命名例外節

**修正前**:
```
**例外**:
- フレームワーク規約 (例: React の `handleClick` イベントハンドラ慣例) は従う。ビジネスロジック側では具体名を用いる。
```

**修正後**:
```
**例外**:
- フレームワーク規約 (例: React の `handleClick` イベントハンドラ慣例) は従う。ビジネスロジック側では具体名を用いる。**判定**: フレームワーク規約適用は「イベント (DOM/フォーム/コンポーネント props 経由) を直接受信する関数」に限る。受信内から呼ぶ業務関数は具体名 (例: `handleDeleteUser` は `deleteUser` を内部で呼ぶ形に分離)。
```

**狙い**:
- 「イベント受信関数」の境界を明確化
- コンポーネント内業務ブリッジ関数の扱いを明示
- subagent の独立解釈コストを削減

## 次アクション

- [x] iter-2.md 作成
- [ ] iter 3: rule L208-209 修正 → 3 並列再評価
- [ ] iter-3.md に結果記録
- [ ] 連続 3/3 達成 → hold-out D
