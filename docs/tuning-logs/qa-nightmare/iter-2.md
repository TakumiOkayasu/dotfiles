# iter 2 — qa-nightmare（関連スキル・境界節追加）

## 変更点（前回差分）

SKILL.md 前提条件節（L28-35）直後に「関連スキル・境界」節を**12行追加**（tdd / test-coverage-guard / e2e-browser / systematic-debugging / consultation 委譲表 + 担当範囲明示）。

## 実行結果（シナリオ別）

| シナリオ | 成功/失敗 | 精度 | steps (tool_uses) | duration | retries |
|---|:---:|---:|---:|---:|---:|
| A（ユーザー登録画面 / PG） | ○ | 95.5% (10.5/11) | 22 | 268.7s | 0 |
| B（注文管理 情報不足） | ○ | 100% (7/7) | 1 | 42.2s | 0 |
| C（通貨マスタ / MySQL / e2e） | ○ | 100% (9/9) | 4 | 93.0s | 0 |
| **平均** |  | **98.5%** | 9.0 | 134.6s | 0 |

全シナリオ `[critical]` 全 ○ で成功判定。

### 前回比（iter 1 → iter 2）

| 指標 | iter 1 | iter 2 | 増減 | 飽和条件 | 判定 |
|---|:---:|:---:|:---:|:---:|:---:|
| 平均精度 | 100% | 98.5% | -1.5pt | +3pt以下 | **○** |
| 平均 tool_uses | 12.0 | 9.0 | -25% | ±10%以内 | × (改善方向) |
| 平均 duration | 405.1s | 134.6s | -66.8% | ±15%以内 | × (改善方向、subagent 環境要因あり) |
| 新規不明瞭点 | — | 4件（QN-2-A-1〜4） | +4 | 0件 | × |

### 要件別詳細

**A（95.5%）**: 要件5 が「部分的」。subagent A は **コンテキスト 86% 到達で全 11 checklist 読込を断念**、カテゴリ名+SKILL.md 既載パターン数から推定適用+`[推測]` マーカー運用。境界節自体の副作用ではなく dispatch 側のコンテキスト制約。境界節追加効果は「Phase 1 情報充足判定」箇所で間接的に効く（Phase 2 直行可の判断）

**B（100%）**: iter 1 と同じく情報不足検出で早期離脱。L103-107「3項目以上 OR スキーマ/権限/状態遷移不明」判定で明快に確認メッセージ生成。境界節の直接引用は発生せず、B のフローは境界節なしでも成立するため期待通り

**C（100%）**: 維持。checklist 部分読み（silent-corruption / auth-bypass のみ精読）で裁量補完、境界節の直接引用は発生しないが、e2e-browser Phase 5 連携手順を全要件達成。iter 1 と同精度維持（tool_uses 23→4 は subagent の判断差、duration 333.7s→93.0s は読込戦略差）

## 不明瞭点（今回新出）

- **QN-2-A-1**: 「カテゴリ名だけで推定適用」の許容度が SKILL.md 側に未規定。コンテキスト逼迫時の fallback 経路が不在
- **QN-2-A-2**: B/C ランクの省略形フォーマットが SKILL.md L179-181 の 6 列表と不整合（dispatch 制約で 1 行に圧縮、SKILL.md 側は全列明示）
- **QN-2-A-3**: B/C の削減基準・合計上限が未規定（過剰列挙のリスク）。Phase 3 L199 「合計: XX ケース」があるが総数上限なし
- **QN-2-A-4**: Phase 2 L120-122 「対策済みであることがコードから明白」のコード参照が評価 dispatch では不可（リポ未提供）、subagent はスキーマ情報+推測で代替

全 4 件は「dispatch 側制約に由来」であり、SKILL.md 構造的曖昧ではない分類

## 裁量補完（今回新出）

- **A**: checklists 本文未読時の推定パターン適用（NM-018/031/032 に `[推測]` マーカー）、B/C 表を 6 列→3 列に圧縮（dispatch 優先）
- **B**: 「把握済み/不足」2 表構成で視覚化、回答不可時のデフォルト推測（EC/単一ロール/標準遷移）を事前提示
- **C**: 非admin ロール名（viewer/editor 仮置き）、物理削除 UI 非提供でも DB 整合性として含める判断、10万件を A→B 降格

## empirical-prompt-tuning 側の曖昧点

- dispatch プロンプトの「必要な checklists/*.md を Read」の必要性判断が subagent 裁量（全読 vs 選択読のガイド不在）
- 「1000行以内」「B/C 1行省略」「spec.ts 25行以内」「11カテゴリ全挙」の同時成立がタイト、subagent は dispatch 優先で SKILL.md テーブル列を圧縮
- 「代表1件」の選定基準（最高スコア / ランダム / ユーザー指定）未規定
- コンテキスト 86% 到達時の subagent 自己判断（一次ソース読込断念 vs 完走）のガイドなし

## 収束判定

- **連続 0/2（境界節追加効果は間接的、新規不明瞭点 4 件で連続カウントリセット）**
- ただし全 4 件は dispatch 側制約由来であり、SKILL.md 構造的曖昧ではない分類
- 精度維持（98.5%、[critical] 全 ○）、duration/tool_uses は改善方向だが subagent 環境要因混在

## 次アクション

- **iter 3 テーマ選定保留**: 新規不明瞭点 QN-2-A-1〜4 はすべて dispatch 側制約由来で、SKILL.md 側の構造修正対象としては弱い
- **判断**: 本 iter-2 で境界節追加の効果（tool_uses/duration 改善方向、精度維持、境界節引用は hold-out で期待）を確認したため、hold-out D へ前倒し移行するか、修正なし iter 3 で再評価する選択肢
- **コンテキスト制約**: 本セッションが 96% 到達のため、iter 3 / hold-out は**次セッションで実施**を推奨
- コミット候補: SKILL.md（境界節追加）+ docs/tuning-logs/qa-nightmare/{scenarios,iter-0,iter-1,iter-2}.md
