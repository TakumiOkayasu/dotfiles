# deep-review 構造審査レポート (empirical 不能のため切替)

## 経緯

iter 1 で 3 シナリオ全ての [critical] #1「3 並列 subagent dispatch」が失敗。原因は **subagent 環境では Task tool が提供されていない** (環境制約)。これは empirical-prompt-tuning skill の「環境制約」節に該当するケースで、構造審査モードに切替。

実環境 (親 Claude Code セッション) では Task tool 有効なため、`/deep-review` 実行時の構造的問題ではない。

## iter 1 (環境制約下の結果、参考値)

| シナリオ | 精度 | [crit] #1 (3 並列) | [crit] #2-3 | 他要件 |
| --- | ---: | :---: | :---: | :---: |
| A (3 観点該当) | 86% (6/7) | × (環境制約) | ○ | 全 ○ |
| B (DB スキーマ + 公開 API) | 86% (6/7) | × (環境制約) | ○ | 全 ○ |
| C (typo 1 文字) | 83% (5/6) | × (環境制約) | ○ | 全 ○ |

[critical] #1 は環境制約による失敗のみ、他 [critical] (#2 修正案添付、#3 判定基準準拠) と他要件は **全シナリオで達成**。指示そのものは subagent が正しく理解し、自前で 3 観点独立走査 + synthesis を実施できた。

## 構造審査結果 (記述レビュー)

審査観点 8 つで `claude/commands/deep-review.md` を静的レビュー、10 件の指摘。

### High 重大度 (1 件、修正推奨)

| # | 行 | 指摘 | 改善案 |
| --- | --- | --- | --- |
| 1 | L58-66 vs L117-123 | 出力フォーマット二重定義 (Step 4 共通 vs 末尾「出力形式」) — 観点タグ有無・連番有無で微妙に異なる | 末尾を正、Step 4 を「subagent 内部出力テンプレ」と明示、synthesis 後に末尾フォーマットへ変換と 1 行追記 |

### Medium 重大度 (5 件)

| # | 行 | 指摘 |
| --- | --- | --- |
| 2 | L48 vs L72 vs L139 | テスト関連の担当境界 (保守性 subagent / tdd subagent / test-writer subagent) が三重化 |
| 3 | L71 vs L100 | パフォーマンス Critical の例が基準表に無い (本番障害級の O(n²) hot path 等を上げる根拠が不明) |
| 4 | L93 | 方針検証発動条件の引用先優先順位 (PR / commit / progress.md / 差分コメント) と「複数跡時の扱い」が未規定 |
| 5 | L70 / L99 | race / TOCTOU がセキュリティ・パフォーマンス・保守性の 3 観点にまたがり責務境界が曖昧 |
| 6 | L36 | diff 取得 fallback (`HEAD` / `HEAD~1` 両方空) の挙動未定義 |

### Low 重大度 (4 件)

| # | 行 | 指摘 |
| --- | --- | --- |
| 7 | L109-111 | Suggestion 件数が判定に影響しない (大量時のフラグ無し) |
| 8 | L99 | 「アーキ依存違反 = Critical」は保守性 subagent が頻発しやすく、BLOCK に振れやすい運用リスク |
| 9 | L131 | 「良い点」禁止は過剰制約の可能性 |
| 10 | L54 / L134 | 「逐次起動禁止」の重複記述 |

### よい点 (保つべき記述)

- L3: 公式 `code-review` プラグインとの差別化を明示
- L28: 「修正案なき指摘は指摘ではない」鉄則の短さ
- L78-81: synthesis 手順 (集約・最高位採用・ソート・偏り検知) の具体性
- L85: 自己検証チェックリストの網羅性
- L125-135: 禁止事項の表化、誤運用予防に効く

## 総評

- 中核強度 (1-5): 4
- 明瞭性 (1-5): 3
- 改善余地: 出力フォーマット二重定義 / race の責務境界 / fallback 補強

## 本セッションでの修正範囲

context 制約から、High 重大度 1 件 (出力フォーマット二重定義) のみ最小修正で対応。Medium/Low 指摘は本ファイルに記録し、別 PR で着手するかユーザー判断に委ねる。

## 別 PR 候補 (将来の改善)

1. Medium #2: テスト担当境界の整理 (deep-review.md L48/L72/L139)
2. Medium #3: パフォーマンス Critical 例を基準表に追加
3. Medium #5: race / TOCTOU の責務境界 1 行追加
4. Medium #6: diff fallback 補強
5. その他は run-on issue として開く
