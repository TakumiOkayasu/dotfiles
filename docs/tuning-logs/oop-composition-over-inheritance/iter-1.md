# iter 1: baseline 3並列 dispatch

## 実施条件

- 対象 rule: `claude/rules/oop-composition-over-inheritance.md` (iter 0 時点、無修正)
- subagent_type: `general-purpose` x 3
- シナリオ: A (通知継承階層リファクタ) / B (税率パラメータ化) / C (フレームワーク継承許容)

## 評価軸 (指示側計測)

| シナリオ | [critical] 全○ | 精度 (%) | tool_uses | duration_ms | 再試行 | 判定 |
|---|---|---|---|---|---|---|
| A | ○ (1,2) | 100.0 (6/6) | 1 | 44,188 | 0 | ○ |
| B | ○ (1,2) | 100.0 (5/5) | 1 | 26,268 | 0 | ○ |
| C | ○ (1,2) | 91.7 (5.5/6) | 2 | 1,335,103 | 0 | ○ (但し部分的 1件) |

- 成功率 (2値): 3/3 ○ (全 [critical] クリア)
- 精度平均: (100 + 100 + 91.7) / 3 = 97.2%
- tool_uses: A/B=1, C=2 (Read + 追加判断)
- duration: A/B は 26-44s で標準範囲。C は 1335s (background 待機含むため純粋な作業時間としては参考値)

## 各シナリオ 実行者自己申告 (抜粋)

### A (通知継承階層リファクタ)
- 成果物: 12クラス継承階層 → `Notifier` Protocol + チャネル3 + Decorator 3 クラスへ分解、`Retrying(Logging(SlackSender))` 形式の合成案
- **不明瞭点: なし** (rule 本文と直結)
- 裁量補完: Python `Protocol` 採用、順序例の意味説明、具体数値 (`max_attempts=3`, `2**attempt`)、例外再 raise ポリシー

### B (税率パラメータ化)
- 成果物: `TaxCalculator(rate=0.10)` 単一クラス案、サブクラス3追加を却下、将来 interface 化の条件 (軽減税率対象判定等) を後置
- **不明瞭点: なし** (L17-28「悪い兆候」L30-38「良い兆候」L40-52「実装のポイント」で判断材料十分)
- 裁量補完: 戻り値 `int` 丸め、型ヒント、private `_rate` 命名

### C (フレームワーク継承許容)
- 成果物: React `Component` / Django `View` + `LoginRequiredMixin` / `Enemy(GameObject)` 全 OK 判定、G1-G6 ガイドライン抽出
- **不明瞭点 3 件**:
  - **C-u1**: 継承深度の定量基準が対象 rule に**無い** (L1-72 全域確認済み)。L15-28 の「悪い兆候」は定性基準のみ。「1-2 段」は rule 外補完
  - **C-u2**: 「合成 > 継承」という明示文言は対象 rule 本文に**無い** (L3-72)。L10-13「合成による拡張性」と L50「外部から合成」から精神を読み取った
  - **C-u3**: L59「Widget/Component の合成 (Flutter, React, SwiftUI)」が `extends Component` 構文を許容する根拠として十分かは解釈に幅あり
- 裁量補完: 継承深度「1-2 段」は一般慣例から補完、React 関数コンポーネント推奨は rule L59 解釈拡張、レビュア回答文面は導出判断
- 再試行: 0

## 観察と課題抽出

### 顕在化した課題

| ID | 内容 | シナリオ | 影響度 | iter 0 予想? |
|---|---|---|---|---|
| **OOP-1-C1** | **フレームワーク継承 / is-a 継承の許容条件が rule 本文に明示されていない** — C は裁量で判断したが subagent 次第で「全継承禁止」と誤発動しうる | C | 高 | はい (OOP-0-5) |
| OOP-1-C2 | 継承深度の定量目安なし (rule 本文に「深い継承禁止」等の定性文言もなし、「2段まで」等の数値もなし) | C | 中 | はい (OOP-0-6) |
| OOP-1-C3 | 「合成 > 継承」の標語が rule 本文に明示なし (精神は全体から読める) | C | 低 | 部分的 (明示文言欠落) |

### 成功要因

- **A / B**: rule L17-28「悪い兆候」L30-38「良い兆候」L40-52「実装のポイント」の 3 節が、継承階層増殖 / パラメータ化 vs サブクラスの判断に**直結**。裁量補完は技術的選択 (言語機能選定・命名) のみで、原則判断は rule 本文で完結
- **rule の意図**は subagent に正しく伝わっている (C も全 OK 判定で結論は正しい) が、**判断根拠の拠り所が rule 外にある**点が C の弱点

### 判定

- 連続クリア候補: 1/2 成立 (現段階で 1 回目 pass)
- ただし C の部分的 (項目 4) + 不明瞭点 3 件は rule 本文の**明示的な継承許容条件欠落**が原因 → iter 2 で修正すれば C も 100% 化可能、かつ将来のより保守的な subagent でも破綻しにくくなる

## iter 2 への方針

**主題: 継承許容条件と深度目安を rule 本文に追記する (OOP-0-5 + OOP-0-6 合流)**

### 追記候補 (位置: L52 「3. 外部から合成する」の後、L53「## 適用範囲」の前)

```markdown
## 継承を採用してよい場面

以下のケースは継承を採用してよい。判断基準は「悪い設計の兆候」(パラメータ増、条件分岐増、責務肥大) に該当しないこと:

- **フレームワーク/ランタイムが継承を要求する基底**: React `Component`, Django `View`, Android `Activity` 等
- **is-a 関係が明確なドメイン階層**: `Enemy is a GameObject`、値オブジェクト階層など
- **Mixin (多重継承形式の合成)**: `LoginRequiredMixin` 等、機能単位を組み合わせる継承構文

**継承深度の目安**: 2 段まで。それ以上は「責務の不明確さ」のリスクが上がるため合成を検討する。機能差分 (能力・振る舞い) は継承ではなく合成 (コンストラクタ注入) で注入する。
```

### 狙い

- OOP-1-C1 (`[critical]` 落ちではないが C 項目 4 の部分的を解消)
- OOP-1-C2 (継承深度の定量目安を本文化)
- C シナリオ再実行で全 ○ + 不明瞭点 0-1 件まで減らす

### 波及予想

- A (継承階層増殖): 新節は**継承許容条件の明示**なので、「増殖は合成へ」の判定は変わらず (むしろ「許容でないケース」の判定材料が増える)
- B (パラメータ化): 新節は継承判断の話、パラメータ化は別軸。影響なし
- C (フレームワーク継承): 新節を直接引用して判定できる → 裁量補完消失

## 次アクション

1. rule 本文に上記節を追記
2. iter 2 として 3 並列再 dispatch (A/B/C の同一シナリオ、新しい subagent)
3. 連続 2/2 を確認できたら hold-out D (単純 DTO) へ進む

## 統計メモ

- duration 外れ値 (C: 1335s): background dispatch での待機時間が大きな割合を占める。比較対象としては A/B の 26-44s が信頼範囲。iter 2 では 3 並列のまま再実行する
