# iter 2 — refactoring (委譲先節を新設)

## 変更点（前回差分）

SKILL.md「禁止事項・制約」表の直後に「## 委譲先（範囲外作業）」節を新設（10 行）。

```markdown
## 委譲先（範囲外作業）

依頼内に以下が混在する場合、本スキルでは扱わず該当スキルに委譲する。委譲時は「本スキルの対象外」を明示してから委譲先を提案する。

| 範囲外作業 | 委譲先スキル |
|---|---|
| テスト新規作成・特性テスト追加（前提条件未充足時） | tdd |
| バグ修正・原因分析（振る舞い変更を伴う） | systematic-debugging → tdd |
| 新規 interface 設計・新規実装 | interface-first-design |
| 既存テストの信頼性検証 | test-coverage-guard |
```

## 実行結果（シナリオ別）

| シナリオ | 成功/失敗 | 精度 | steps | duration | retries |
|---|---|---|---|---|---|
| A（長いメソッド分割） | ○ | 100% (6/6) | 1 | 49.6s | 0 |
| B（テスト不在で依頼） | ○ | 100% (6/6) | 1 | 45.8s | 0 |
| C（ついで修正混入） | ○ | 100% (6/6) | 1 | **423.4s** | 0 |

**平均**: 精度100% / tool_uses 全1 (均一) / duration 172.9s / retries 全0

### duration 警告

シナリオC で iter 1 比 +9 倍の duration。応答内容は正常（精度 100%、tool_uses=1、retries=0、品質劣化なし）。**外れ値（subagent 側の一過性遅延）と判断**。A: +5.5% / B: +14.7% は ±15% 閾値内。iter 3 で C のみ再観測して傾向確認。

## 不明瞭点（今回新出）

### シナリオA
- iter 1 と同じ「省略部の具体ロジック不明」のみ。**新規ゼロ**
- 補足: 「コメント除去」を変更#3 として実施。スメル例にないが「過度な圧縮禁止」に抵触しない範囲と裁量判定（既出 R-1-A-2 系）

### シナリオB
- **不明瞭点: なし** と subagent が明示。iter 2 の委譲先表が直接機能し、`tdd` への委譲を構造的に決定 → R-1-B-1 / R-1-B-2 解消

### シナリオC
- **R-2-C-1**: 既存テストが `* 1.10` の結果を固定しているか保証なし（特性テストの粒度不明） → R-1-A-1 系の延長
- 補足: `reduce` 化を「過度な圧縮・巧妙化」禁止に抵触する可能性ありと自己抑制 → 候補（仮）として提示しユーザー確認に委ねる

## 解消された曖昧点

| ID | 出処 | 解消手段 |
|---|---|---|
| R-0-2 | iter 0 予想 | 委譲先表が委譲条件を明文化 |
| R-0-4 | iter 0 予想 | 「テスト新規作成・特性テスト追加 → tdd」で含意 |
| R-1-B-1 | iter 1 シナリオB | 委譲先表に `tdd` 明記 |
| R-1-B-2 | iter 1 シナリオB | 委譲先表で「本スキル対象外」を明示する規約化 |
| R-1-C-1 | iter 1 シナリオC | 委譲先表で `systematic-debugging` / `interface-first-design` を明示 |

iter 1 で挙げた 6 件の懸念のうち **5 件解消**。残 R-1-A-1 / R-1-A-2 / R-1-C-2（マジックナンバー定数化境界）は iter 3 で対処判定。

## 裁量補完（今回新出）

| シナリオ | 補完内容 |
|---|---|
| A | 抽出メソッド名（指示文例示採用）、TS 慣例の `private static readonly`、コメント除去を変更#3 として追加 |
| B | 特性テストの分岐パターン例の列挙、選択肢A/B 形式での意思決定促進 |
| C | PR #1/#2/#3 の番号付け、PR #2/#3 順序（バグ修正 → 機能追加）、`reduce` 化候補の自己抑制提示 |

## empirical-prompt-tuning 側の曖昧点

- A: 「実施宣言」の粒度（見出しか明示文か）/ 要件5 の "等" の許容範囲
- B: 要件4「説明する」の粒度 / 要件6「TDD 委譲」が必須か「必要なら」か
- C: 要件3「対象列挙」が抽象記述で足りるか具体スメルまで必要か（フェーズ手順上はテスト確認後）

→ Phase 4 (empirical 自身の構造審査) で蓄積参照。

## 次の修正案

### 採用テーマ: **マジックナンバー定数化と仕様変更の境界明文化**

**修正内容**: SKILL.md「コードスメル」節「マジックナンバー → 定数」（L83-92）の直下に 1 行追記:

```markdown
**境界**: 値を据え置いたままの定数化は構造改善（本スキル対象）。値変更・引数化・切替機能化は仕様変更（→ 委譲先表参照）。
```

**根拠**:
- R-1-A-2（iter 1 A）/ R-1-C-2（iter 1 C）/ R-2-C 補足（reduce 化抑制）の 3 件が同じ「振る舞い不変境界」の構造的曖昧
- iter 2 委譲先表で「委譲先」は明確化されたが、**何を委譲するか** の判定基準が「マジックナンバー定数化」付近で曖昧残存
- 修正サイズ最小（1 行追記、既存節への局所追加）

### 不採用テーマ
- **R-2-C-1（特性テスト粒度）**: シナリオB の「特性テスト」言及で含意済、シナリオC では subagent が確認要求として穏当処理。明文化の追加価値は低い
- **「過度な圧縮禁止」の例示拡充**: 1 件のみの観察、修正サイズ大（複数例示が必要）

## 収束判定

- 連続クリア 1/2 達成（精度100% 維持、新規不明瞭点 = R-2-C-1 のみで既出系延長）
- 典型スキル目標: 連続 2 / hold-out 通過
- iter 3 で精度 100% 維持 + R-1-A-2 / R-1-C-2 系の解消なら 2/2 達成 → hold-out 評価へ
- duration C は iter 3 で再観測（外れ値判定の確証取り）
