# premise-questioning iter 4: D 由来 2 件解消

**実施日**: 2026-04-30
**対応ブランチ**: `chore/empirical-tuning-iter3-axis-format` (iter 3 と同ブランチで継続)
**修正テーマ**: iter 3 hold-out (シナリオ D = useEffect ESLint auto-fix) で識別された D 由来 2 件 (D.1 / D.2) を解消

## Context

iter 3 hold-out 評価で識別された skill 由来 2 論点:

1. **D.1 簡易モード時の hold-out 要否**: skill には「3 ラウンド全 ✅ で採用判定が出た直後」の手順しか書かれておらず、簡易モード (1 ラウンド + 自己採点) で 🔴 確定時 / 🟡 単独時の hold-out 要否が不明
2. **D.2 「採点根拠の引用形式」(a)(b) を両方か択一か**: skill 文言「いずれかを 1 行で書く」と運用 (本セッションでの (a)(b) 計 6 行) に齟齬

## 修正内容

### D.1: 簡易モード時の hold-out 要否を明文化

`claude/skills/premise-questioning/SKILL.md`:

**簡易モード節 (L205) 直後に新項目追加**:
- 簡易モード時の過適合チェック (hold-out) は **不要**
- 理由: 過適合チェックは 3 ラウンド全 ✅ 採用判定後の手順、1 ラウンドのみの簡易モードは構造的に対象外
- 確信が持てない場合は通常モード (3 ラウンド) に切り替えて再評価

**過適合チェック節 (L219) を「通常モードのみ」と書き換え**:
- 適用条件を明示: 通常モード (3 ラウンド) で全 ✅ 採用判定が出た直後のみ
- 簡易モード (1 ラウンド) / 🔴 撤回判定 / 🟡 単独一致時は対象外 (構造的に hold-out 意味なし)
- 手順 (逆方針 1 ラウンド回す) は維持

### D.2: 「採点根拠の引用形式」を「(a) 必須 / (b) 併記推奨」に変更

`claude/skills/premise-questioning/SKILL.md` L153-158、および `claude/skills/feature-pruning/SKILL.md` L177-182 を同型に書換:

- (a) 判定文言引用 **(必須)**: 「スコア軸」節の該当点の文言を逐語引用
- (b) 数値根拠 **(具体数値が出せる場合は必須)**: 推定使用率 / 暗黙仮定数 / 影響範囲 等の具体値
- (b) を「具体数値が出せない」と省略する場合、採点が判定文言からの推測に留まっている可能性が高く、**再 dispatch 案件**

これにより iter 3 評価で全 subagent が両方記載していた運用と skill 文言が整合。

## 動作テスト

新規 dispatch なし。iter 3 hold-out (D) で発生した曖昧さに対する skill 文言適用を点検:

| 論点 | iter 3 hold-out で発生 | iter 4 修正後の skill 文言適用 | 結果 |
|---|---|---|---|
| D.1 簡易モード hold-out 要否 | subagent が「🔴 確定時は不要と解釈 (skill には明示なし)」と裁量 | 「簡易モードは過適合チェック不要」「通常モード 3 ラウンド全 ✅ のみ適用」を一意に判定可能 | ✅ 解消 |
| D.2 (a)(b) 両方か択一か | skill 「いずれか 1 行」 vs 運用 「(a)(b) 計 6 行」 で齟齬 | 「(a) 必須 / (b) 数値が出せる場合は必須」で運用と整合、(b) なしは再 dispatch 案件 | ✅ 解消 |

## 動作テストを subagent dispatch で行わない理由

- iter 3 で 4 シナリオ (B / A / C / hold-out D) を dispatch 済み、修正テーマ (A.1 / A.2 / E.1) は全シナリオで活用確認済
- 本 iter は明文化のみ (採点ロジック / 結論ラベル変更なし)
- 簡易モード / hold-out 要否は構造規定で、新規 dispatch せずとも文言で完結

## 収束判定

- iter 3 hold-out D 由来 2 論点を全件解消
- iter 4 残候補なし
- **判定: ✅ 収束**

## 次アクション

- [x] SKILL.md 修正 (D.1 = 2 箇所 / D.2 = 1 箇所、計 3 箇所 Edit、feature 側 D.2 同型修正 1 箇所も同期)
- [x] iter-4.md 作成
- [ ] PROGRESS.md に iter 4 結果転記
- [ ] iter 3 + iter 4 を同一コミット or 別コミットにするかユーザー判断
