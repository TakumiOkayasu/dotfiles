# premise-questioning iter 3: A.1 / A.2 / E.1 共通解消

**実施日**: 2026-04-30
**対応ブランチ**: `chore/empirical-tuning-iter3-axis-format`
**修正テーマ**: 採点軸文言の閾値化 (4/2 補完) + 採点根拠引用形式の明文化 + マトリクス集約ルール (ばらつき判定) の節立て

## Context

iter 2 (1761644 / PR #287) で 80 点判断で収束扱いとしていたが、両 skill (premise-questioning + feature-pruning) 横断で次の構造的曖昧さが iter 2 残しとして識別されていた:

- **A.1 Q-Validity 文言主観性**: 各軸 5/3/1/0 のみで 4 と 2 が欠番、中間値判定が主観評価のみ
- **A.2 採点根拠引用形式**: `<採点根拠>` placeholder のみで引用形式未規定
- **E.1 提示フォーマット変換ルール**: 9 セル (3R × 3軸) のばらつき判定閾値が打ち切り基準節と提示フォーマット節で重複・不一致

iter 3 は両 skill 並行で同型修正を入れる別建て iter。本ログは premise-questioning 側の記録 (feature-pruning 側は `docs/tuning-logs/feature-pruning/iter-3.md`)。

## 修正内容

### A.1: Q-Validity / S-Necessity / A-Soundness に 4/2 補完

`claude/skills/premise-questioning/SKILL.md:155-180` を 6 段階 (5/4/3/2/1/0) に補完。各 4/2 文言は **測定可能な閾値** (代替手段の段数 / 暗黙仮定数 / 影響範囲) を含む形にした:

| 軸 | 4 (新規) | 2 (新規) |
|---|---|---|
| Q-Validity | 価値に直結するが、現状の運用回避で 1 サイクルは凌げる (恒久対処は必要) | 一部ユーザーの不便があるが、放置しても主要動線には影響しない |
| S-Necessity | 実装が最短経路だが、設定 / 運用回避にも 1 段階の追加コストで到達可能 | 部分的に必要、半分以上は設定 / 運用で代替できる |
| A-Soundness | 暗黙仮定が 1 個あるが影響は局所的、検出も容易 (テスト / ログで顕在化) | 暗黙仮定が複数あるが、いずれも検出時点で局所修正可能 |

### A.2: 「採点根拠の引用形式」節を起動契約直後に新設

`claude/skills/premise-questioning/SKILL.md:151` 直後に新節追加:

```
## 採点根拠の引用形式
各軸の採点根拠は次のいずれかを 1 行で書く:
- (a) 判定文言引用: 「スコア軸」節の該当点の文言を引く
- (b) 数値根拠: 推定使用率 / 暗黙仮定数 / 影響範囲 等の具体値で示す
抽象表現 (「重要そう」「便利そう」) は NG。
```

### E.1: 提示フォーマット内に「マトリクス集約ルール (ばらつき判定)」節を追加

`claude/skills/premise-questioning/SKILL.md:223` 直前に集約ルール節を追加。premise-questioning の表は元々 9 セル全表示型のため、feature-pruning と異なり「中央値 1 セル化」は構造的に適用不能。代わりに **ばらつき判定の閾値統一** (打ち切り基準節 L196-200 と整合) として趣旨を踏襲した:

- ±1 以内 → 収束気味 (該当軸は 3 ラウンド中央値で確定)
- ±2 以上 → 拡張ラウンド発動 (打ち切り基準節を参照)

例示「ばらつき分析」も「軸ごとの中央値 + ばらつき値 + 推定原因」の形に統一。

## 実行結果 (シナリオ B = JWT → OAuth2 移行方針 / 中央値、3 ラウンド逐次)

| シナリオ | 結論 | retries | duration | tool_uses | skill 由来不明瞭点 |
|---|:---:|:---:|---:|:---:|:---:|
| B (JWT → OAuth2) | 🔴 撤回検討 (3R 一致) | 0 | 114.6s | 1 | 0 |

各ラウンド採点 (Q-Val / S-Nec / A-Snd):
- Round 1 (第一原理): 3 / 2 / 2 → 🔴
- Round 2 (Inversion): 3 / 1 / 1 → 🔴
- Round 3 (5 Whys): 2 / 1 / 2 → 🔴
- 軸ばらつき (max-min): Q-Val=1 / S-Nec=1 / A-Snd=1 (全て ±1 以内、収束気味)
- 中央値: Q-Val=3 / S-Nec=1 / A-Snd=2 (S-Nec=1 が致命傷の隠蔽防止ルールに該当 → 撤回判定補強)

## 要件達成 ([critical] 必達 3 項目)

| 項目 | 達成 | 備考 |
|---|:---:|---|
| C-A1: 4/2 採点で中間値文言を引用 | ○ | スコア 2 が複数登場 (Q-Val=2 / S-Nec=2 / A-Snd=2)、各々判定文言を直接引用 |
| C-A2: 全 9 セル採点根拠が (a)/(b) 形式 1 行 | ○ | 9 セル全てに (a) 判定文言引用 or (b) 数値根拠 (例「月 30 件 / 5000 名 = 0.6%」「メアド一致率 80% / Google 95% / GitHub 30% の 3 個の暗黙仮定」) を 1 行で記述 |
| C-E1: 集約マトリクスがばらつき判定節準拠 | ○ | 統合提示で軸ごと中央値 + ばらつき値 + 推定原因を明記、ばらつき分析行も skill 例示と同型 |

## 不明瞭点 (今回新出)

skill 由来 0 件。下記 3 件は **skill 修正対象外の軽微な解釈差** (subagent 自己申告):

1. 「結論根拠 2-3 行」(ラウンド) と「採用なら根拠 3 行」(統合) の整合 → 両方満たす書き方を採用 (subagent 裁量)
2. hold-out チェックの適用範囲 → 「3 ラウンド全 ✅ で採用判定が出た直後」と明記のため、🔴 確定時は不要と解釈 (skill には「🔴 で hold-out 不要」明示なし、解釈一致)
3. 「マトリクス集約ルール」節の「中央値で確定」が、採点を中央値に丸めるのか表示を中央値にするのか軽微に不明瞭 → 表示として解釈 (実害なし)

→ いずれも skill 修正案件ではなく、解釈で吸収可能なレベル。

## 裁量補完 (今回新出)

- 失敗シナリオ 5 個の具体内容 (Inversion ラウンド) はシナリオ B の前提から推論で構築
- 5 Whys の各 Why 連鎖は subagent が自由度で構築
- セルフサービス強化での「月 30 件 → 月 5 件以下」数値推定は経験則に基づく裁量
- hold-out を撤回判定時に省略する判断 (skill が採用判定時のみ明記しているため)

## 収束判定

- iter 2 比 (シナリオ C 中央値): retries 0 維持、duration ±15% 以内 (114.6s ↔ iter 2 平均 67.5s 程度から増加)
- skill 由来不明瞭点: iter 2 = 0 / iter 3 = 0 (連続 2 回クリア達成、empirical L132 「収束 (停止)」基準満たす)
- 修正テーマ 3 件全て活用 (4/2 文言 / 採点根拠 (a)(b) / 集約ルール ばらつき判定)
- **判定: ✅ 収束** (連続 2 回クリア + 80 点)。残作業は hold-out 過適合チェック

## 次アクション

- [x] SKILL.md 修正 (A.1 / A.2 / E.1)
- [ ] empirical iter 3 dispatch 完了後、本ログの結果セクション更新
- [ ] hold-out 過適合チェック (シナリオ D = useEffect ESLint)
- [ ] 連続 2 回クリア達成判定
- [ ] PROGRESS.md に iter 3 結果転記
