# iter 0 — failure-logging（description / body 整合チェック）

静的レビュー。dispatch は行わない。empirical-prompt-tuning「Iteration 0」節準拠。

## 対象

- `claude/skills/failure-logging/SKILL.md`（108行）
- description: 「エラー発生時、アプローチ失敗時に使用。失敗DBを構築し同じ失敗を繰り返さない。」

## description が謳う内容

| 要素 | 内容 |
|---|---|
| トリガー語 | **エラー発生時** / **アプローチ失敗時** |
| 目的 | **失敗DBを構築** / **同じ失敗を繰り返さない** |

## body 側の対応状況

### カバーされているもの（description → body の片方向）

| description 要素 | body 該当箇所 | 状態 |
|---|---|---|
| エラー発生時 | L12「コマンドがエラーになった時」 | ○ |
| アプローチ失敗時 | L13「試したアプローチがうまくいかなかった時」 | ○ |
| 失敗DBを構築 | フェーズ1（L29-50）+ ファイル配置規約 L22-24 | ○ |
| 同じ失敗を繰り返さない | フェーズ2（L54-62）+ 禁止事項 L103-105 | ○ |

description の両面（トリガー + 目的）は body に完全カバーされている。乖離は検出されない。

### body にあるが description に無いもの（body 拡張）

| body 要素 | description 未言及度 |
|---|---|
| トリガー L14「同じ問題で2回以上つまずいた時」 | description の「アプローチ失敗時」の具体化。乖離ではなく具体化 |
| トリガー L15「新しいアプローチを試みる直前」 | フェーズ2（履歴参照）の起点。description「同じ失敗を繰り返さない」の実現手段 |
| トリガー L16「`/compact` 後」 | 文脈リセットケース。description の「失敗DBを構築」の文脈継続性を支える。**description だけでは読み取り困難**。iter 1 で subagent が自然発動するか観察 |
| フェーズ3（解決時の更新, L66-79） | description が「失敗DB構築」止まりで、**解決マーク運用が description からは予測困難**。ただし body が手順を提供しているので発動条件は明確 |

## 隣接スキルとの境界（trigger-overlap.md 🟢 低リスク行）

| 語 | 隣接スキル | 現状 | 懸念 |
|---|---|---|---|
| 失敗 | systematic-debugging（分析） | body に明示なし | 🟡 middle — シナリオD（バグ原因究明依頼）で failure-logging を過剰発動するリスクあり。description「同じ失敗を繰り返さない」は **記録** の話で、**分析** は責務外。境界節が body にないため subagent が委譲判断に迷う可能性 |
| テスト失敗 | tdd / systematic-debugging | body に明示なし | 🟢 low — 「テスト」語は description に無く、トリガー L12 の「コマンドエラー」の一事例として読める。過剰発動リスクは低 |
| `/compact` 後 | （なし） | L16 独自 | ○ 差別化要素。他スキルが `/compact` トリガーを持たないため衝突なし |

## 構造的観察（iter 1 dispatch 前の仮説）

| # | 観察項目 | 予想される症状 | iter 観察対象 |
|---|---|---|---|
| FL-0-1 | 境界節が body に無い | シナリオD（hold-out）で systematic-debugging 委譲が曖昧になる可能性 | hold-out 時に判定 |
| FL-0-2 | 連番継続規則（L23）の文言が「既存ファイルの最大連番から継続する」のみ | 新規ディレクトリ時の開始番号（1 なのか 0 なのか）が未規定 | シナリオA で subagent が「1 から開始」または「0 から開始」の判断に迷うか |
| FL-0-3 | 100件到達の判定タイミング（L50「100件到達時は新ファイルを作成」） | 100 件目の記録が `1-100` に入るのか `101-200` に入るのかが曖昧 | シナリオA/B/C では 100 件境界に触れないため今回は観察対象外 |
| FL-0-4 | フェーズ2 の「関連する失敗エントリを検索する」（L58） | 検索手段（grep か全文 Read か）が未規定。`claude_tmp/failure_log/` が複数ファイルに分かれた場合の探索手順は subagent 裁量 | シナリオB で `tool_uses` 偏りが出るか観察 |
| FL-0-5 | フェーズ3（解決時の更新）が「該当エントリに マークする」（L68） | 既存エントリの in-place 書き換え時、`## 72` ヘッダ行の更新ルールが「## 72 ✅ 解決済み」で明記されている（L73 サンプル）のでズレにくい | シナリオC で観察 |
| FL-0-6 | 「履歴参照完了時」の出力形式（L93-95）の「除外手法」の書き方 | 「除外手法: [手法名]」の粒度（コマンド文字列 / 概念語）が未規定 | シナリオB で観察 |
| FL-0-7 | description の「失敗DB」という語 | body では「`claude_tmp/failure_log/` 配下の連番ファイル群」を指すが、DB 製品（SQLite 等）を連想する subagent がいないか | iter 1 で観察 |

## 判定

- description ↔ body の**乖離は検出されなかった**。両面の整合はとれている
- iter 1（baseline dispatch）に進める
- **iter 0 で修正は入れない**。境界節（FL-0-1）は hold-out D の結果を見て iter 2+ テーマとして判断
- FL-0-2 / FL-0-4 / FL-0-6 は body 自身の曖昧さだが、シナリオで実害が顕在化してから対応（empirical「iter 0 修正は乖離があるときのみ」の精神）
