# iter 2 — failure-logging（`[件数]` / `[手法名]` 定義注釈を追加）

## 変更点（前回差分）

`claude/skills/failure-logging/SKILL.md` L93-95「参照完了時は以下を出力する」ブロックに 1 行追記:

```diff
 参照完了時は以下を出力する:

 ```
 📋 履歴確認済み: [件数]件の関連失敗を確認。除外手法: [手法名]
 ```
+
+`[件数]` = 関連キーワードでヒットした全エントリ数（解決済み含む）。`[手法名]` = そのうち未解決エントリの「試した手法/コマンド」欄に記載された手法（カンマ区切り、無ければ「なし」）。
```

- テーマ: **FL-1-B-2（`[件数]` 定義が 3 通りに解釈可能）** を解消
- 修正範囲: +1 行（表示上は +3 行、空行含む）
- 目的: iter 1 のシナリオB + empirical 側 EP-1-B-1 の二重指摘を同時解消（上振れ期待）

## 実行結果（シナリオ別）

| シナリオ | 成功/失敗 | 精度 | steps | duration | retries |
|---|:---:|---:|---:|---:|---:|
| A（コマンド失敗→記録→次アプローチ） | ○ | 100% (7/7) | 1 | 40.9s | 0 |
| B（`/compact` 後→履歴参照）         | ○ | 100% (7/7) | 1 | 43.4s | 0 |
| C（解決時の更新）                   | ○ | 100% (6/6) | 1 | 26.1s | 0 |

- 平均: 精度 100%（20/20）、tool_uses 1.0、duration 36.8s
- 前回比: 精度 **±0pt**、tool_uses **0%**、duration **+7.9%**（いずれも飽和条件内: 精度 +3pt以下 / tool_uses ±10% / duration ±15%）

## 追記が機能した実地観察

シナリオB subagent が iter 2 追記部分を**直接引用**:

> 「SKILL.md L97 に『関連キーワードでヒットした全エントリ数（解決済み含む）』と定義があるためそれを採用した」

→ iter 1 で「3 通りに解釈可能」と報告されていた曖昧が、iter 2 では subagent の判断根拠として**構造的に機能**。

シナリオA subagent も同様に「『解決済み含む全エントリ数』と定義されているため」と引用。裁量補完から**明文化された選択肢**への格上げを 2 subagent 独立で観察。

## 不明瞭点（今回新出）

### SKILL.md 起因の新出
- **シナリオA**: なし（iter 1 の FL-1-A-1 継続、FL-1-A-2 継続 だが新出はなし）
- **シナリオB**: なし
- **シナリオC**: なし（iter 1 の FL-1-C-1「解決方法の詳細度」継続のみ）

**SKILL.md 起因の新規不明瞭点は 0 件**。iter 1 から継続する曖昧はあるが、いずれも subagent が裁量で正解に到達しており実害なし。

### empirical-prompt-tuning 側の新出（Phase 4 送り）
- **EP-2-A-1（シナリオA）**: 「過去の失敗DB内容（`1-100-fail.md` 中身）」が与えられていないため実検索結果は仮値でしか示せない
- **EP-2-A-2（シナリオA）**: 「次アプローチ案をいくつ提示すべきか」が empirical 契約で未指定
- **EP-2-A-3（シナリオA）**: 「擬似実行かテキスト提示か」の線引きが曖昧（指示では「ファイル作成しない」と明記したが、出力文の形式まで指定せず）
- **EP-2-B-1（シナリオB）**: シナリオ前提の「137件」とキーワードヒット件数の区別が紛らわしい
- **EP-2-B-2（シナリオB）**: 要件チェックリストの「等」の範囲で ○ / 部分的 の境界不明
- **EP-2-C-1（シナリオC）**: 「出力形式サマリ」をレポートに含めるか empirical 契約に未明記

※ 6 件すべて empirical-prompt-tuning / scenarios.md / subagent 契約側の曖昧。対象 skill (failure-logging) 本体の問題ではない

## 裁量補完（今回新出）

- シナリオA: 原因欄に「コンテナ名衝突」と明記（エラーメッセージ `Conflict. The container name "/app" is already in use` が原因を直接述べるため「明確」と解釈 = iter 1 と同じ裁量判断、正解）
- シナリオA: 次アプローチ 4 案に `docker ps --filter` 調査ステップを追加（iter 1 の 3 案から増加、シナリオ裁量範囲）
- シナリオB: キーワードに `413`（Payload Too Large）/ `CORS` / `timeout` を追加（iter 1 の 5 分類からさらに拡張、裁量範囲）
- シナリオC: 解決方法文面「VPN を有効化した状態で `bun install` を再実行したところ、レジストリへ到達でき成功した」（iter 1 と同方向、文言は異なるが意味は同じ）

## 収束判定

| 条件 | iter 1 → iter 2 | 判定 |
|---|---|:---:|
| 新規不明瞭点（SKILL.md 起因） | 0 件 | ○ |
| 精度の前回比改善 | ±0pt（+3pt 以下） | ○ |
| tool_uses の前回比変動 | 0%（±10% 以内） | ○ |
| duration の前回比変動 | +7.9%（±15% 以内） | ○ |

**連続クリアカウント: 1/2 達成**（典型スキル目標）

### iter 3 方針

**修正なし再評価**で連続 2/2 を確認する。根拠:

- SKILL.md 起因の新規不明瞭点が 0 件で「構造的欠陥の顕在化は iter 2 で収束方向」
- FL-1-B-2（iter 2 テーマ）の効果は subagent 直接引用で確証取れているが、subagent 1 ロット での検証。iter 3 で別 subagent 3本で再現性を確認
- 残存 FL-1-A-1 / FL-1-A-2 / FL-1-C-1 はいずれも裁量許容範囲、追加修正すると肥大化リスク
- 先行スキル（tdd iter 4 / qa-nightmare iter 4 / measure iter 3）と同型の「修正なし再評価で連続2/3達成」パターンを踏襲

停止条件まであと: 1 iter（iter 3 で連続 2 達成 → hold-out へ）
